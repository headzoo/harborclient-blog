<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" ><generator uri="https://jekyllrb.com/" version="4.4.1">Jekyll</generator><link href="http://localhost:4001/feed.xml" rel="self" type="application/atom+xml" /><link href="http://localhost:4001/" rel="alternate" type="text/html" /><updated>2026-06-24T09:56:58-04:00</updated><id>http://localhost:4001/feed.xml</id><title type="html">Application-to-Web Internals</title><subtitle>Discover the world of REST APIs with our comprehensive notes and insights. From basic concepts to advanced techniques, we provide valuable resources for developers and enthusiasts alike.</subtitle><author><name>Sean</name></author><entry><title type="html">Postman Alternatives</title><link href="http://localhost:4001/rest-api/testing/tools/2026/06/24/postman-alternatives.html" rel="alternate" type="text/html" title="Postman Alternatives" /><published>2026-06-24T08:00:00-04:00</published><updated>2026-06-24T08:00:00-04:00</updated><id>http://localhost:4001/rest-api/testing/tools/2026/06/24/postman-alternatives</id><content type="html" xml:base="http://localhost:4001/rest-api/testing/tools/2026/06/24/postman-alternatives.html"><![CDATA[<p>Postman is the default API client for a lot of teams. It is polished, familiar, and covers most day-to-day REST testing needs. But not every project fits the same workflow. Pricing, required accounts, cloud sync, privacy requirements, or a preference for lighter tools often push developers to look elsewhere.</p>

<p>This guide surveys practical Postman alternatives — from desktop apps that keep your data local to browser and editor tools that fit different workflows. None of these tools need to replace Postman entirely; the right choice depends on how you work, who you collaborate with, and where you want your collections to live.</p>

<h2 id="why-teams-look-beyond-postman">Why teams look beyond Postman</h2>

<p>Common reasons developers switch or supplement Postman include:</p>

<ul>
  <li><strong>Cost and licensing</strong> — per-seat plans add up for larger teams</li>
  <li><strong>Account and cloud requirements</strong> — some workflows need offline or self-hosted options</li>
  <li><strong>Data ownership</strong> — collections tied to a vendor account are harder to move or audit</li>
  <li><strong>Workflow fit</strong> — git-based collections, CLI automation, or in-editor testing may match your stack better</li>
  <li><strong>Feature scope</strong> — a simpler tool can be faster when you only need request/response testing</li>
</ul>

<p>The alternatives below address different combinations of these needs.</p>

<h2 id="harborclient">HarborClient</h2>

<p><a href="https://harborclient.com/">HarborClient</a> is a free, open-source desktop API client for macOS, Windows, and Linux. It offers a familiar Postman-style workspace — collections, environments, request scripts, tests, and a tabbed editor — while keeping your work on your machine or on storage you control. There are no accounts, subscriptions, or required cloud sync.</p>

<h3 id="what-harborclient-offers">What HarborClient offers</h3>

<ul>
  <li><strong>Collections and environments</strong> — organize saved requests, share variables across a collection, and switch global environment groups from the tab bar</li>
  <li><strong>Request builder</strong> — method selector, URL bar, query params, headers, and body (JSON or plain text)</li>
  <li><strong>Scripts and tests</strong> — pre- and post-request JavaScript at the collection or request level; post-request tests with assertions shown in a Tests tab</li>
  <li><strong>Variable substitution</strong> — use <code class="language-plaintext highlighter-rouge">{{variable}}</code> placeholders in URLs, headers, params, body, and scripts</li>
  <li><strong>Tabbed workspace</strong> — open multiple requests side by side with independent drafts and responses</li>
</ul>

<h3 id="storage-and-collaboration">Storage and collaboration</h3>

<p>HarborClient defaults to local SQLite and supports pluggable backends:</p>

<ul>
  <li><strong>Local SQLite</strong> — collections and environments stay on your machine by default</li>
  <li><strong>Remote databases</strong> — optional MySQL, PostgreSQL, or Firestore when you need shared storage you operate</li>
  <li><strong>Git-backed collections</strong> — version-controlled collection files you can commit and push from the app</li>
  <li><strong>Team Hub</strong> — self-hosted collaboration with token-based access, without sharing database credentials across teammates</li>
  <li><strong>Encrypted sharing</strong> — signed share tokens for remote collections using RSA keys you manage</li>
</ul>

<h3 id="migrating-from-postman">Migrating from Postman</h3>

<p>HarborClient imports <strong>Postman v2.1</strong> collection exports. After install, use <strong>Collections → Import</strong> or <strong>File → Import</strong> to bring existing work over. Some Postman-specific settings and scripts may need adjustment after import — HarborClient does not claim full feature parity — but the core request, variable, and collection structure usually transfers cleanly.</p>

<p><strong>Best for:</strong> individual developers and teams that want a real desktop app, local or self-hosted data, and a Postman-like workflow without vendor lock-in.</p>

<p>Downloads and docs: <a href="https://harborclient.com/">harborclient.com</a> · <a href="https://github.com/headzoo/harborclient/releases/latest">GitHub releases</a></p>

<h2 id="insomnia">Insomnia</h2>

<p><a href="https://insomnia.rest/">Insomnia</a> is a dedicated REST and GraphQL API client with a clean interface and strong support for multiple auth types, environment variables, and request chaining. It targets developers who want a polished, standalone app focused on API design and exploration rather than broader platform features.</p>

<p>Insomnia organizes work into <strong>design documents</strong> and <strong>collections</strong>, supports GraphQL queries alongside REST, and includes plugins for extending behavior. Cloud sync and team features are available on paid plans; local-only use remains possible for individual workflows.</p>

<p><strong>Best for:</strong> developers who need REST and GraphQL in one polished desktop client and are comfortable with Insomnia’s account and sync model.</p>

<h2 id="bruno">Bruno</h2>

<p><a href="https://www.usebruno.com/">Bruno</a> stores API collections as plain files in a folder on disk — typically committed to git. There is no proprietary cloud sync; collaboration happens through branches, pull requests, and your existing version-control workflow.</p>

<p>Bruno supports environments, scripting, and a straightforward request editor. Because collections are files, diffs and reviews in git are natural. The trade-off is less built-in team infrastructure than account-based clients provide.</p>

<p><strong>Best for:</strong> teams that want collections in the repo next to application code and prefer git as the collaboration layer.</p>

<h2 id="httpie">HTTPie</h2>

<p><a href="https://httpie.io/">HTTPie</a> started as a friendly command-line HTTP client (<code class="language-plaintext highlighter-rouge">http GET api.example.com/users</code>) and expanded into a desktop app and other tooling. The CLI remains popular for quick one-off requests, scripting, and automation in shell pipelines.</p>

<p>For developers who already live in the terminal, HTTPie can be faster than opening a GUI for exploratory calls. The desktop app adds a visual layer when you want forms and response inspection without leaving the HTTPie ecosystem.</p>

<p><strong>Best for:</strong> CLI-first workflows, scripting, and developers who want minimal friction for ad hoc HTTP calls.</p>

<h2 id="hoppscotch">Hoppscotch</h2>

<p><a href="https://hoppscotch.com/">Hoppscotch</a> is a browser-based API client for quick REST, GraphQL, and WebSocket testing. No install is required for the web app — open a tab, paste a URL, and send a request.</p>

<p>Hoppscotch fits spur-of-the-moment exploration, demos, and environments where installing desktop software is awkward. Collections and workspaces exist, but the tool is optimized for speed and accessibility rather than deep team collection management.</p>

<p><strong>Best for:</strong> fast browser-based exploration and lightweight testing without a local install.</p>

<h2 id="thunder-client">Thunder Client</h2>

<p><a href="https://www.thunderclient.com/">Thunder Client</a> is a VS Code extension that brings REST API testing into the editor. Collections, environments, and test scripts live inside your IDE workspace, which suits developers who want to test APIs while coding without switching applications.</p>

<p>Thunder Client covers common HTTP methods, auth, and collection organization. It integrates with the editor’s UI patterns — sidebar panels, keyboard shortcuts, and workspace-scoped storage — so API tests stay close to the code they exercise.</p>

<p><strong>Best for:</strong> VS Code users who want API testing embedded in their editor workflow.</p>

<h2 id="how-to-choose">How to choose</h2>

<p>Rather than ranking tools on every feature, match the tool to your constraints:</p>

<table>
  <thead>
    <tr>
      <th>If you need…</th>
      <th>Consider…</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>Local data, no accounts, open source, Postman-like desktop app</td>
      <td>HarborClient</td>
    </tr>
    <tr>
      <td>REST + GraphQL in a polished standalone client</td>
      <td>Insomnia</td>
    </tr>
    <tr>
      <td>Collections as git files in your repository</td>
      <td>Bruno</td>
    </tr>
    <tr>
      <td>Terminal and scripting workflows</td>
      <td>HTTPie</td>
    </tr>
    <tr>
      <td>Zero-install browser testing</td>
      <td>Hoppscotch</td>
    </tr>
    <tr>
      <td>API testing inside VS Code</td>
      <td>Thunder Client</td>
    </tr>
    <tr>
      <td>Full platform features, monitors, and broad enterprise tooling</td>
      <td>Postman (still a valid choice)</td>
    </tr>
  </tbody>
</table>

<p>Also weigh <strong>collaboration model</strong>: vendor cloud sync, shared database, git repos, self-hosted hub, or no sharing at all. And weigh <strong>automation depth</strong>: post-request test scripts are enough for many teams; larger CI pipelines may still rely on code-based integration tests regardless of which client you use for manual exploration.</p>

<h2 id="closing-thoughts">Closing thoughts</h2>

<p>There is no single best Postman alternative. HarborClient stands out when privacy, local or self-hosted storage, and a free desktop workflow matter. Bruno fits git-centric teams. Hoppscotch and HTTPie cover quick and CLI paths. Thunder Client keeps testing inside the editor. Insomnia remains a strong option when you want a dedicated API client with GraphQL support.</p>

<p>Pick based on where your collections should live, how your team collaborates, and whether you prefer desktop, browser, editor, or command-line tools. Start with one endpoint in the client that matches your workflow, import or recreate a small collection, and expand from there — the same progression that works with Postman applies to every alternative on this list.</p>]]></content><author><name>Sean</name></author><category term="rest-api" /><category term="testing" /><category term="tools" /><summary type="html"><![CDATA[Postman is the default API client for a lot of teams. It is polished, familiar, and covers most day-to-day REST testing needs. But not every project fits the same workflow. Pricing, required accounts, cloud sync, privacy requirements, or a preference for lighter tools often push developers to look elsewhere.]]></summary><media:thumbnail xmlns:media="http://search.yahoo.com/mrss/" url="http://localhost:4001/assets/images/postman-alternatives.png" /><media:content medium="image" url="http://localhost:4001/assets/images/postman-alternatives.png" xmlns:media="http://search.yahoo.com/mrss/" /></entry><entry><title type="html">How to test REST APIs with Postman</title><link href="http://localhost:4001/rest-api/testing/2026/06/23/how-to-test-rest-apis-with-postman.html" rel="alternate" type="text/html" title="How to test REST APIs with Postman" /><published>2026-06-23T17:00:00-04:00</published><updated>2026-06-23T17:00:00-04:00</updated><id>http://localhost:4001/rest-api/testing/2026/06/23/how-to-test-rest-apis-with-postman</id><content type="html" xml:base="http://localhost:4001/rest-api/testing/2026/06/23/how-to-test-rest-apis-with-postman.html"><![CDATA[<p>Postman is one of the fastest ways to explore and validate a REST API without writing a full client. You can send requests, inspect responses, and repeat the same checks across environments — all from a single workspace.</p>

<p>This guide walks through a practical workflow for testing REST APIs with Postman, from your first request to a small reusable test suite.</p>

<h2 id="why-use-postman-for-rest-apis">Why use Postman for REST APIs</h2>

<p>REST APIs are tested by sending HTTP requests and verifying the responses. Postman gives you a visual interface for that work, which makes it easy to:</p>

<ul>
  <li>Try endpoints before frontend or integration code exists</li>
  <li>Share example requests with teammates</li>
  <li>Save headers, auth tokens, and base URLs in one place</li>
  <li>Automate basic response checks with test scripts</li>
</ul>

<p>For quick manual testing and lightweight automation, Postman is often enough on its own.</p>

<h2 id="create-your-first-request">Create your first request</h2>

<p>Start with a simple <code class="language-plaintext highlighter-rouge">GET</code> request:</p>

<ol>
  <li>Open Postman and create a new request.</li>
  <li>Set the method to <code class="language-plaintext highlighter-rouge">GET</code>.</li>
  <li>Enter the URL, for example <code class="language-plaintext highlighter-rouge">https://jsonplaceholder.typicode.com/posts/1</code>.</li>
  <li>Click <strong>Send</strong>.</li>
</ol>

<p>You should see a <code class="language-plaintext highlighter-rouge">200 OK</code> response with a JSON body. That is the basic loop for every REST test: build a request, send it, inspect the result.</p>

<h2 id="set-method-url-headers-and-body">Set method, URL, headers, and body</h2>

<p>Most REST APIs use a small set of HTTP methods:</p>

<table>
  <thead>
    <tr>
      <th>Method</th>
      <th>Typical use</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">GET</code></td>
      <td>Read a resource</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">POST</code></td>
      <td>Create a resource</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">PUT</code> / <code class="language-plaintext highlighter-rouge">PATCH</code></td>
      <td>Update a resource</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">DELETE</code></td>
      <td>Remove a resource</td>
    </tr>
  </tbody>
</table>

<p>For each request, configure the parts that matter:</p>

<ul>
  <li><strong>URL</strong> — include path parameters, such as <code class="language-plaintext highlighter-rouge">/users/42</code></li>
  <li><strong>Query params</strong> — add filters or pagination, such as <code class="language-plaintext highlighter-rouge">?page=2&amp;limit=10</code></li>
  <li><strong>Headers</strong> — set <code class="language-plaintext highlighter-rouge">Content-Type: application/json</code> for JSON bodies, and <code class="language-plaintext highlighter-rouge">Accept: application/json</code> when needed</li>
  <li><strong>Body</strong> — use raw JSON for <code class="language-plaintext highlighter-rouge">POST</code> and <code class="language-plaintext highlighter-rouge">PUT</code> requests</li>
</ul>

<p>Example JSON body for creating a post:</p>

<div class="language-json highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="p">{</span><span class="w">
  </span><span class="nl">"title"</span><span class="p">:</span><span class="w"> </span><span class="s2">"Test post"</span><span class="p">,</span><span class="w">
  </span><span class="nl">"body"</span><span class="p">:</span><span class="w"> </span><span class="s2">"Created from Postman"</span><span class="p">,</span><span class="w">
  </span><span class="nl">"userId"</span><span class="p">:</span><span class="w"> </span><span class="mi">1</span><span class="w">
</span><span class="p">}</span><span class="w">
</span></code></pre></div></div>

<p>Send the request and confirm the API returns the expected status code and payload shape.</p>

<h2 id="inspect-the-response">Inspect the response</h2>

<p>After each request, check three things:</p>

<ol>
  <li><strong>Status code</strong> — <code class="language-plaintext highlighter-rouge">200</code>, <code class="language-plaintext highlighter-rouge">201</code>, <code class="language-plaintext highlighter-rouge">204</code>, <code class="language-plaintext highlighter-rouge">400</code>, <code class="language-plaintext highlighter-rouge">401</code>, and <code class="language-plaintext highlighter-rouge">404</code> all mean different things</li>
  <li><strong>Headers</strong> — look for <code class="language-plaintext highlighter-rouge">Content-Type</code>, rate-limit headers, and auth-related values</li>
  <li><strong>Body</strong> — verify fields, types, and error messages</li>
</ol>

<p>Postman shows all of this in the response panel. For JSON responses, use the <strong>Pretty</strong> view to scan the structure quickly.</p>

<p>A healthy test usually answers questions like:</p>

<ul>
  <li>Did the request succeed?</li>
  <li>Did the response include the fields I expected?</li>
  <li>Did the API return a useful error when I sent bad input?</li>
</ul>

<h2 id="use-environments-and-variables">Use environments and variables</h2>

<p>Hard-coding URLs and tokens in every request gets old fast. Postman environments solve that.</p>

<p>Create an environment with variables such as:</p>

<ul>
  <li><code class="language-plaintext highlighter-rouge">baseUrl</code> — <code class="language-plaintext highlighter-rouge">https://api.example.com/v1</code></li>
  <li><code class="language-plaintext highlighter-rouge">accessToken</code> — your bearer token or API key</li>
</ul>

<p>Then reference them in requests:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>{{baseUrl}}/users
</code></pre></div></div>

<p>And in headers:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Authorization: Bearer {{accessToken}}
</code></pre></div></div>

<p>Switch environments to move between local, staging, and production without rewriting requests. That alone saves a lot of time on real projects.</p>

<h2 id="add-test-scripts">Add test scripts</h2>

<p>Postman can run JavaScript after each response. This turns a manual check into a repeatable assertion.</p>

<p>Example test script:</p>

<div class="language-javascript highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nx">pm</span><span class="p">.</span><span class="nf">test</span><span class="p">(</span><span class="dl">"</span><span class="s2">Status code is 200</span><span class="dl">"</span><span class="p">,</span> <span class="nf">function </span><span class="p">()</span> <span class="p">{</span>
  <span class="nx">pm</span><span class="p">.</span><span class="nx">response</span><span class="p">.</span><span class="nx">to</span><span class="p">.</span><span class="nx">have</span><span class="p">.</span><span class="nf">status</span><span class="p">(</span><span class="mi">200</span><span class="p">);</span>
<span class="p">});</span>

<span class="nx">pm</span><span class="p">.</span><span class="nf">test</span><span class="p">(</span><span class="dl">"</span><span class="s2">Response is JSON</span><span class="dl">"</span><span class="p">,</span> <span class="nf">function </span><span class="p">()</span> <span class="p">{</span>
  <span class="nx">pm</span><span class="p">.</span><span class="nx">response</span><span class="p">.</span><span class="nx">to</span><span class="p">.</span><span class="nx">be</span><span class="p">.</span><span class="nx">json</span><span class="p">;</span>
<span class="p">});</span>

<span class="nx">pm</span><span class="p">.</span><span class="nf">test</span><span class="p">(</span><span class="dl">"</span><span class="s2">Post has an id</span><span class="dl">"</span><span class="p">,</span> <span class="nf">function </span><span class="p">()</span> <span class="p">{</span>
  <span class="kd">const</span> <span class="nx">json</span> <span class="o">=</span> <span class="nx">pm</span><span class="p">.</span><span class="nx">response</span><span class="p">.</span><span class="nf">json</span><span class="p">();</span>
  <span class="nx">pm</span><span class="p">.</span><span class="nf">expect</span><span class="p">(</span><span class="nx">json</span><span class="p">).</span><span class="nx">to</span><span class="p">.</span><span class="nx">have</span><span class="p">.</span><span class="nf">property</span><span class="p">(</span><span class="dl">"</span><span class="s2">id</span><span class="dl">"</span><span class="p">);</span>
<span class="p">});</span>
</code></pre></div></div>

<p>Use the <strong>Test Results</strong> tab to see what passed or failed. Even a few small tests are enough to catch regressions when an endpoint changes.</p>

<h2 id="organize-requests-into-collections">Organize requests into collections</h2>

<p>As your API grows, group related requests into a <strong>Collection</strong>. A good layout might look like:</p>

<ul>
  <li><code class="language-plaintext highlighter-rouge">Users</code>
    <ul>
      <li>List users</li>
      <li>Get user by id</li>
      <li>Create user</li>
    </ul>
  </li>
  <li><code class="language-plaintext highlighter-rouge">Posts</code>
    <ul>
      <li>List posts</li>
      <li>Create post</li>
    </ul>
  </li>
</ul>

<p>Collections make it easier to:</p>

<ul>
  <li>Run the same set of requests in order</li>
  <li>Share API examples with your team</li>
  <li>Export documentation from saved requests</li>
</ul>

<p>If you are testing auth flows, put login and token refresh requests at the top of the collection so other requests can reuse the token.</p>

<h2 id="a-simple-testing-checklist">A simple testing checklist</h2>

<p>Before you call an endpoint done, run through this list:</p>

<ul class="task-list">
  <li class="task-list-item"><input type="checkbox" class="task-list-item-checkbox" disabled="disabled" />Correct HTTP method and URL</li>
  <li class="task-list-item"><input type="checkbox" class="task-list-item-checkbox" disabled="disabled" />Required headers and auth configured</li>
  <li class="task-list-item"><input type="checkbox" class="task-list-item-checkbox" disabled="disabled" />Valid request body for create/update operations</li>
  <li class="task-list-item"><input type="checkbox" class="task-list-item-checkbox" disabled="disabled" />Expected success status code</li>
  <li class="task-list-item"><input type="checkbox" class="task-list-item-checkbox" disabled="disabled" />Response schema matches the contract</li>
  <li class="task-list-item"><input type="checkbox" class="task-list-item-checkbox" disabled="disabled" />Error cases return clear status codes and messages</li>
  <li class="task-list-item"><input type="checkbox" class="task-list-item-checkbox" disabled="disabled" />Variables used for base URL, tokens, and IDs</li>
  <li class="task-list-item"><input type="checkbox" class="task-list-item-checkbox" disabled="disabled" />Test scripts cover the most important assertions</li>
</ul>

<h2 id="closing-thoughts">Closing thoughts</h2>

<p>Postman is not a replacement for full integration tests in your application, but it is an excellent tool for exploring APIs, validating contracts, and building a shared set of working examples.</p>

<p>Start with one endpoint, add variables for anything repeated, then layer in test scripts as the API stabilizes. That progression keeps testing lightweight while still giving you confidence that your REST API behaves the way you expect.</p>]]></content><author><name>Sean</name></author><category term="rest-api" /><category term="testing" /><summary type="html"><![CDATA[Postman is one of the fastest ways to explore and validate a REST API without writing a full client. You can send requests, inspect responses, and repeat the same checks across environments — all from a single workspace.]]></summary><media:thumbnail xmlns:media="http://search.yahoo.com/mrss/" url="http://localhost:4001/assets/images/how-to-test-rest-apis-with-postman.png" /><media:content medium="image" url="http://localhost:4001/assets/images/how-to-test-rest-apis-with-postman.png" xmlns:media="http://search.yahoo.com/mrss/" /></entry><entry><title type="html">Building Type-Safe REST API Clients in TypeScript</title><link href="http://localhost:4001/rest-api/typescript/clients/2026/06/10/building-type-safe-rest-api-clients-in-typescript.html" rel="alternate" type="text/html" title="Building Type-Safe REST API Clients in TypeScript" /><published>2026-06-10T11:35:00-04:00</published><updated>2026-06-10T11:35:00-04:00</updated><id>http://localhost:4001/rest-api/typescript/clients/2026/06/10/building-type-safe-rest-api-clients-in-typescript</id><content type="html" xml:base="http://localhost:4001/rest-api/typescript/clients/2026/06/10/building-type-safe-rest-api-clients-in-typescript.html"><![CDATA[<p>TypeScript can make REST API clients easier to use and safer to refactor, but only if the types describe the real API contract. A typed wrapper around <code class="language-plaintext highlighter-rouge">fetch</code> should prevent common mistakes without hiding how HTTP works.</p>

<p>The goal is not to build a giant SDK on day one. Start with small, typed functions for the endpoints your application uses most.</p>

<h2 id="define-response-types-at-the-boundary">Define response types at the boundary</h2>

<p>Begin with the shapes your client expects:</p>

<div class="language-typescript highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kd">type</span> <span class="nx">User</span> <span class="o">=</span> <span class="p">{</span>
  <span class="na">id</span><span class="p">:</span> <span class="kr">string</span><span class="p">;</span>
  <span class="nl">email</span><span class="p">:</span> <span class="kr">string</span><span class="p">;</span>
  <span class="nl">displayName</span><span class="p">:</span> <span class="kr">string</span><span class="p">;</span>
  <span class="nl">createdAt</span><span class="p">:</span> <span class="kr">string</span><span class="p">;</span>
<span class="p">};</span>

<span class="kd">type</span> <span class="nx">ApiError</span> <span class="o">=</span> <span class="p">{</span>
  <span class="na">error</span><span class="p">:</span> <span class="p">{</span>
    <span class="na">code</span><span class="p">:</span> <span class="kr">string</span><span class="p">;</span>
    <span class="nl">message</span><span class="p">:</span> <span class="kr">string</span><span class="p">;</span>
    <span class="nl">details</span><span class="p">?:</span> <span class="nb">Array</span><span class="o">&lt;</span><span class="p">{</span>
      <span class="nx">field</span><span class="p">?:</span> <span class="kr">string</span><span class="p">;</span>
      <span class="nl">message</span><span class="p">:</span> <span class="kr">string</span><span class="p">;</span>
    <span class="p">}</span><span class="o">&gt;</span><span class="p">;</span>
  <span class="p">};</span>
<span class="p">};</span>
</code></pre></div></div>

<p>These types are a contract for your application code. Keep them close to the client module or generate them from OpenAPI if your API publishes a reliable spec.</p>

<h2 id="wrap-fetch-once">Wrap fetch once</h2>

<p>A small request helper keeps headers, base URLs, JSON parsing, and error handling consistent.</p>

<div class="language-typescript highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kd">type</span> <span class="nx">RequestOptions</span> <span class="o">=</span> <span class="p">{</span>
  <span class="nx">method</span><span class="p">?:</span> <span class="kr">string</span><span class="p">;</span>
  <span class="nl">path</span><span class="p">:</span> <span class="kr">string</span><span class="p">;</span>
  <span class="nl">token</span><span class="p">?:</span> <span class="kr">string</span><span class="p">;</span>
  <span class="nl">body</span><span class="p">?:</span> <span class="nx">unknown</span><span class="p">;</span>
<span class="p">};</span>

<span class="k">async</span> <span class="kd">function</span> <span class="nf">request</span><span class="o">&lt;</span><span class="nx">T</span><span class="o">&gt;</span><span class="p">(</span><span class="nx">options</span><span class="p">:</span> <span class="nx">RequestOptions</span><span class="p">):</span> <span class="nb">Promise</span><span class="o">&lt;</span><span class="nx">T</span><span class="o">&gt;</span> <span class="p">{</span>
  <span class="kd">const</span> <span class="nx">response</span> <span class="o">=</span> <span class="k">await</span> <span class="nf">fetch</span><span class="p">(</span><span class="s2">`</span><span class="p">${</span><span class="nx">API_BASE_URL</span><span class="p">}${</span><span class="nx">options</span><span class="p">.</span><span class="nx">path</span><span class="p">}</span><span class="s2">`</span><span class="p">,</span> <span class="p">{</span>
    <span class="na">method</span><span class="p">:</span> <span class="nx">options</span><span class="p">.</span><span class="nx">method</span> <span class="o">??</span> <span class="dl">"</span><span class="s2">GET</span><span class="dl">"</span><span class="p">,</span>
    <span class="na">headers</span><span class="p">:</span> <span class="p">{</span>
      <span class="dl">"</span><span class="s2">Accept</span><span class="dl">"</span><span class="p">:</span> <span class="dl">"</span><span class="s2">application/json</span><span class="dl">"</span><span class="p">,</span>
      <span class="dl">"</span><span class="s2">Content-Type</span><span class="dl">"</span><span class="p">:</span> <span class="dl">"</span><span class="s2">application/json</span><span class="dl">"</span><span class="p">,</span>
      <span class="p">...(</span><span class="nx">options</span><span class="p">.</span><span class="nx">token</span> <span class="p">?</span> <span class="p">{</span> <span class="dl">"</span><span class="s2">Authorization</span><span class="dl">"</span><span class="p">:</span> <span class="s2">`Bearer </span><span class="p">${</span><span class="nx">options</span><span class="p">.</span><span class="nx">token</span><span class="p">}</span><span class="s2">`</span> <span class="p">}</span> <span class="p">:</span> <span class="p">{})</span>
    <span class="p">},</span>
    <span class="na">body</span><span class="p">:</span> <span class="nx">options</span><span class="p">.</span><span class="nx">body</span> <span class="p">?</span> <span class="nx">JSON</span><span class="p">.</span><span class="nf">stringify</span><span class="p">(</span><span class="nx">options</span><span class="p">.</span><span class="nx">body</span><span class="p">)</span> <span class="p">:</span> <span class="kc">undefined</span>
  <span class="p">});</span>

  <span class="k">if </span><span class="p">(</span><span class="o">!</span><span class="nx">response</span><span class="p">.</span><span class="nx">ok</span><span class="p">)</span> <span class="p">{</span>
    <span class="k">throw</span> <span class="k">await</span> <span class="nx">response</span><span class="p">.</span><span class="nf">json</span><span class="p">();</span>
  <span class="p">}</span>

  <span class="k">return</span> <span class="nx">response</span><span class="p">.</span><span class="nf">json</span><span class="p">()</span> <span class="kd">as </span><span class="nb">Promise</span><span class="o">&lt;</span><span class="nx">T</span><span class="o">&gt;</span><span class="p">;</span>
<span class="p">}</span>
</code></pre></div></div>

<p>The generic <code class="language-plaintext highlighter-rouge">T</code> gives callers a typed result. The helper still needs runtime care because TypeScript does not validate JSON automatically.</p>

<h2 id="add-endpoint-functions">Add endpoint functions</h2>

<p>Endpoint functions give the rest of your app a stable API.</p>

<div class="language-typescript highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kd">type</span> <span class="nx">CreateUserInput</span> <span class="o">=</span> <span class="p">{</span>
  <span class="na">email</span><span class="p">:</span> <span class="kr">string</span><span class="p">;</span>
  <span class="nl">displayName</span><span class="p">:</span> <span class="kr">string</span><span class="p">;</span>
<span class="p">};</span>

<span class="k">export</span> <span class="kd">function</span> <span class="nf">getUser</span><span class="p">(</span><span class="nx">id</span><span class="p">:</span> <span class="kr">string</span><span class="p">,</span> <span class="nx">token</span><span class="p">:</span> <span class="kr">string</span><span class="p">)</span> <span class="p">{</span>
  <span class="k">return</span> <span class="nx">request</span><span class="o">&lt;</span><span class="nx">User</span><span class="o">&gt;</span><span class="p">({</span>
    <span class="na">path</span><span class="p">:</span> <span class="s2">`/users/</span><span class="p">${</span><span class="nf">encodeURIComponent</span><span class="p">(</span><span class="nx">id</span><span class="p">)}</span><span class="s2">`</span><span class="p">,</span>
    <span class="nx">token</span>
  <span class="p">});</span>
<span class="p">}</span>

<span class="k">export</span> <span class="kd">function</span> <span class="nf">createUser</span><span class="p">(</span><span class="nx">input</span><span class="p">:</span> <span class="nx">CreateUserInput</span><span class="p">,</span> <span class="nx">token</span><span class="p">:</span> <span class="kr">string</span><span class="p">)</span> <span class="p">{</span>
  <span class="k">return</span> <span class="nx">request</span><span class="o">&lt;</span><span class="nx">User</span><span class="o">&gt;</span><span class="p">({</span>
    <span class="na">method</span><span class="p">:</span> <span class="dl">"</span><span class="s2">POST</span><span class="dl">"</span><span class="p">,</span>
    <span class="na">path</span><span class="p">:</span> <span class="dl">"</span><span class="s2">/users</span><span class="dl">"</span><span class="p">,</span>
    <span class="nx">token</span><span class="p">,</span>
    <span class="na">body</span><span class="p">:</span> <span class="nx">input</span>
  <span class="p">});</span>
<span class="p">}</span>
</code></pre></div></div>

<p>Now application code calls <code class="language-plaintext highlighter-rouge">getUser()</code> instead of constructing URLs and headers everywhere.</p>

<h2 id="model-pagination-explicitly">Model pagination explicitly</h2>

<p>If the API wraps list responses, type that wrapper once:</p>

<div class="language-typescript highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kd">type</span> <span class="nx">Page</span><span class="o">&lt;</span><span class="nx">T</span><span class="o">&gt;</span> <span class="o">=</span> <span class="p">{</span>
  <span class="na">data</span><span class="p">:</span> <span class="nx">T</span><span class="p">[];</span>
  <span class="nl">pagination</span><span class="p">:</span> <span class="p">{</span>
    <span class="na">limit</span><span class="p">:</span> <span class="kr">number</span><span class="p">;</span>
    <span class="nl">nextCursor</span><span class="p">?:</span> <span class="kr">string</span><span class="p">;</span>
    <span class="nl">hasMore</span><span class="p">:</span> <span class="nx">boolean</span><span class="p">;</span>
  <span class="p">};</span>
<span class="p">};</span>

<span class="kd">type</span> <span class="nx">ListUsersParams</span> <span class="o">=</span> <span class="p">{</span>
  <span class="nx">limit</span><span class="p">?:</span> <span class="kr">number</span><span class="p">;</span>
  <span class="nl">cursor</span><span class="p">?:</span> <span class="kr">string</span><span class="p">;</span>
<span class="p">};</span>
</code></pre></div></div>

<p>Build query strings from typed params:</p>

<div class="language-typescript highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kd">function</span> <span class="nf">listUsers</span><span class="p">(</span><span class="nx">params</span><span class="p">:</span> <span class="nx">ListUsersParams</span><span class="p">,</span> <span class="nx">token</span><span class="p">:</span> <span class="kr">string</span><span class="p">)</span> <span class="p">{</span>
  <span class="kd">const</span> <span class="nx">search</span> <span class="o">=</span> <span class="k">new</span> <span class="nc">URLSearchParams</span><span class="p">();</span>

  <span class="k">if </span><span class="p">(</span><span class="nx">params</span><span class="p">.</span><span class="nx">limit</span><span class="p">)</span> <span class="nx">search</span><span class="p">.</span><span class="nf">set</span><span class="p">(</span><span class="dl">"</span><span class="s2">limit</span><span class="dl">"</span><span class="p">,</span> <span class="nc">String</span><span class="p">(</span><span class="nx">params</span><span class="p">.</span><span class="nx">limit</span><span class="p">));</span>
  <span class="k">if </span><span class="p">(</span><span class="nx">params</span><span class="p">.</span><span class="nx">cursor</span><span class="p">)</span> <span class="nx">search</span><span class="p">.</span><span class="nf">set</span><span class="p">(</span><span class="dl">"</span><span class="s2">cursor</span><span class="dl">"</span><span class="p">,</span> <span class="nx">params</span><span class="p">.</span><span class="nx">cursor</span><span class="p">);</span>

  <span class="k">return</span> <span class="nx">request</span><span class="o">&lt;</span><span class="nx">Page</span><span class="o">&lt;</span><span class="nx">User</span><span class="o">&gt;&gt;</span><span class="p">({</span>
    <span class="na">path</span><span class="p">:</span> <span class="s2">`/users?</span><span class="p">${</span><span class="nx">search</span><span class="p">.</span><span class="nf">toString</span><span class="p">()}</span><span class="s2">`</span><span class="p">,</span>
    <span class="nx">token</span>
  <span class="p">});</span>
<span class="p">}</span>
</code></pre></div></div>

<p>This prevents misspelled parameters from spreading through the codebase.</p>

<h2 id="treat-errors-as-part-of-the-contract">Treat errors as part of the contract</h2>

<p>Do not leave errors as <code class="language-plaintext highlighter-rouge">any</code>. Give them a shape:</p>

<div class="language-typescript highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kd">class</span> <span class="nc">ApiClientError</span> <span class="kd">extends</span> <span class="nc">Error</span> <span class="p">{</span>
  <span class="nf">constructor</span><span class="p">(</span>
    <span class="nx">message</span><span class="p">:</span> <span class="kr">string</span><span class="p">,</span>
    <span class="k">public</span> <span class="nx">status</span><span class="p">:</span> <span class="kr">number</span><span class="p">,</span>
    <span class="k">public</span> <span class="nx">code</span><span class="p">?:</span> <span class="kr">string</span><span class="p">,</span>
    <span class="k">public</span> <span class="nx">details</span><span class="p">?:</span> <span class="nx">ApiError</span><span class="p">[</span><span class="dl">"</span><span class="s2">error</span><span class="dl">"</span><span class="p">][</span><span class="dl">"</span><span class="s2">details</span><span class="dl">"</span><span class="p">]</span>
  <span class="p">)</span> <span class="p">{</span>
    <span class="k">super</span><span class="p">(</span><span class="nx">message</span><span class="p">);</span>
  <span class="p">}</span>
<span class="p">}</span>
</code></pre></div></div>

<p>Then throw a typed error from the request helper. UI and service code can handle <code class="language-plaintext highlighter-rouge">validation_failed</code>, <code class="language-plaintext highlighter-rouge">token_expired</code>, or <code class="language-plaintext highlighter-rouge">rate_limit_exceeded</code> without parsing unknown objects in every caller.</p>

<h2 id="validate-runtime-data-when-it-matters">Validate runtime data when it matters</h2>

<p>Type assertions do not prove the server returned the right shape:</p>

<div class="language-typescript highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">return</span> <span class="nx">response</span><span class="p">.</span><span class="nf">json</span><span class="p">()</span> <span class="kd">as </span><span class="nb">Promise</span><span class="o">&lt;</span><span class="nx">User</span><span class="o">&gt;</span><span class="p">;</span>
</code></pre></div></div>

<p>That line tells TypeScript what you expect. It does not validate the JSON.</p>

<p>For critical boundaries, add runtime validation with your project’s existing validation library or generated client tooling. If the project does not already use a validator, weigh the dependency carefully instead of adding one casually.</p>

<h2 id="generate-types-when-the-spec-is-trustworthy">Generate types when the spec is trustworthy</h2>

<p>If your API has a maintained OpenAPI spec, generated types can reduce manual drift.</p>

<p>Generation works best when:</p>

<ul>
  <li>The spec is part of CI</li>
  <li>Examples match real responses</li>
  <li>Breaking changes are reviewed</li>
  <li>Generated files are easy to update</li>
  <li>Custom client code stays thin</li>
</ul>

<p>Generated clients are not magic. They are only as accurate as the contract they come from.</p>

<h2 id="closing-thoughts">Closing thoughts</h2>

<p>A type-safe REST client should make correct calls easy and incorrect calls hard. Start with typed endpoint functions, one request helper, explicit pagination types, and a real error model.</p>

<p>As the API grows, consider OpenAPI-generated types or runtime validation for the highest-risk boundaries. Keep the wrapper small enough that developers still understand the HTTP request being made.</p>]]></content><author><name>Sean</name></author><category term="rest-api" /><category term="typescript" /><category term="clients" /><summary type="html"><![CDATA[TypeScript can make REST API clients easier to use and safer to refactor, but only if the types describe the real API contract. A typed wrapper around fetch should prevent common mistakes without hiding how HTTP works.]]></summary><media:thumbnail xmlns:media="http://search.yahoo.com/mrss/" url="http://localhost:4001/assets/images/building-type-safe-rest-api-clients-in-typescript.png" /><media:content medium="image" url="http://localhost:4001/assets/images/building-type-safe-rest-api-clients-in-typescript.png" xmlns:media="http://search.yahoo.com/mrss/" /></entry><entry><title type="html">Contract Testing for REST APIs</title><link href="http://localhost:4001/rest-api/testing/contracts/2026/05/04/contract-testing-for-rest-apis.html" rel="alternate" type="text/html" title="Contract Testing for REST APIs" /><published>2026-05-04T04:55:00-04:00</published><updated>2026-05-04T04:55:00-04:00</updated><id>http://localhost:4001/rest-api/testing/contracts/2026/05/04/contract-testing-for-rest-apis</id><content type="html" xml:base="http://localhost:4001/rest-api/testing/contracts/2026/05/04/contract-testing-for-rest-apis.html"><![CDATA[<p>REST APIs fail when providers and consumers disagree about the contract. Maybe a field is renamed, a status code changes, or an error response stops matching the documentation. Unit tests can pass while real clients break.</p>

<p>Contract testing focuses on the boundary between API provider and API consumer. It asks: do both sides still agree on requests, responses, and behavior?</p>

<h2 id="what-is-an-api-contract">What is an API contract?</h2>

<p>An API contract describes what clients can rely on.</p>

<p>It includes:</p>

<ul>
  <li>Paths and methods</li>
  <li>Request headers, query parameters, and bodies</li>
  <li>Response status codes</li>
  <li>Response schemas</li>
  <li>Error formats</li>
  <li>Authentication requirements</li>
  <li>Pagination and rate-limit behavior</li>
</ul>

<p>An OpenAPI document is a common way to publish that contract, but the contract also includes examples, docs, and behavior that clients depend on.</p>

<h2 id="schema-validation-is-the-first-layer">Schema validation is the first layer</h2>

<p>The simplest contract test checks that responses match a schema.</p>

<p>For example, a <code class="language-plaintext highlighter-rouge">GET /users/{id}</code> response might require:</p>

<div class="language-json highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="p">{</span><span class="w">
  </span><span class="nl">"id"</span><span class="p">:</span><span class="w"> </span><span class="s2">"usr_123"</span><span class="p">,</span><span class="w">
  </span><span class="nl">"email"</span><span class="p">:</span><span class="w"> </span><span class="s2">"avery@example.com"</span><span class="p">,</span><span class="w">
  </span><span class="nl">"created_at"</span><span class="p">:</span><span class="w"> </span><span class="s2">"2026-05-04T08:55:00Z"</span><span class="w">
</span><span class="p">}</span><span class="w">
</span></code></pre></div></div>

<p>A test can verify:</p>

<ul>
  <li>Required fields are present</li>
  <li>Field types are correct</li>
  <li>Unknown breaking changes are caught</li>
  <li>Status codes match the spec</li>
</ul>

<p>This is useful, but it is not the whole story. A valid schema can still represent the wrong behavior.</p>

<h2 id="provider-contract-tests">Provider contract tests</h2>

<p>Provider tests run against the API implementation and compare actual responses to the contract.</p>

<p>They answer questions like:</p>

<ul>
  <li>Does every documented endpoint exist?</li>
  <li>Does the API return documented status codes?</li>
  <li>Do response bodies match the OpenAPI schema?</li>
  <li>Do error responses use the shared error shape?</li>
</ul>

<p>These tests are a good fit for CI because they catch drift between code and docs before release.</p>

<h2 id="consumer-contract-tests">Consumer contract tests</h2>

<p>Consumer contract tests start from what a client actually needs.</p>

<p>A frontend, mobile app, or partner integration defines expectations such as:</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>When I request GET /users/me with a valid token,
I need a 200 response with id, email, and display_name.
</code></pre></div></div>

<p>The provider verifies that it can satisfy those expectations. This is especially useful when many clients depend on the same API and not every client uses every field.</p>

<h2 id="contract-tests-are-not-end-to-end-tests">Contract tests are not end-to-end tests</h2>

<p>End-to-end tests exercise a full user workflow. Contract tests focus on the API boundary.</p>

<p>Contract tests should be:</p>

<ul>
  <li>Faster than full E2E tests</li>
  <li>Focused on request and response behavior</li>
  <li>Stable enough to run in CI</li>
  <li>Clear when a breaking change is introduced</li>
</ul>

<p>They do not replace integration tests, database tests, or browser tests. They reduce the chance that a client breaks because the API shape changed unexpectedly.</p>

<h2 id="use-examples-as-test-fixtures">Use examples as test fixtures</h2>

<p>Good API docs include real examples. Those examples can become fixtures.</p>

<p>Example request:</p>

<div class="language-http highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="err">POST /users
Content-Type: application/json
</span></code></pre></div></div>

<div class="language-json highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="p">{</span><span class="w">
  </span><span class="nl">"email"</span><span class="p">:</span><span class="w"> </span><span class="s2">"avery@example.com"</span><span class="p">,</span><span class="w">
  </span><span class="nl">"name"</span><span class="p">:</span><span class="w"> </span><span class="s2">"Avery"</span><span class="w">
</span><span class="p">}</span><span class="w">
</span></code></pre></div></div>

<p>Expected response:</p>

<div class="language-http highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">HTTP</span><span class="o">/</span><span class="m">1.1</span> <span class="m">201</span> <span class="ne">Created</span>
</code></pre></div></div>

<div class="language-json highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="p">{</span><span class="w">
  </span><span class="nl">"id"</span><span class="p">:</span><span class="w"> </span><span class="s2">"usr_123"</span><span class="p">,</span><span class="w">
  </span><span class="nl">"email"</span><span class="p">:</span><span class="w"> </span><span class="s2">"avery@example.com"</span><span class="p">,</span><span class="w">
  </span><span class="nl">"name"</span><span class="p">:</span><span class="w"> </span><span class="s2">"Avery"</span><span class="w">
</span><span class="p">}</span><span class="w">
</span></code></pre></div></div>

<p>When examples are executable, documentation drift becomes easier to catch.</p>

<h2 id="watch-for-backwards-compatibility">Watch for backwards compatibility</h2>

<p>Contract tests should protect existing clients from breaking changes.</p>

<p>Risky changes include:</p>

<ul>
  <li>Removing fields</li>
  <li>Renaming fields</li>
  <li>Changing types</li>
  <li>Changing enum values</li>
  <li>Changing pagination wrappers</li>
  <li>Changing error response shape</li>
  <li>Returning different status codes for the same condition</li>
</ul>

<p>Additive changes are usually safer. A new optional response field should not break well-built clients, but strict clients may still need attention.</p>

<h2 id="keep-test-data-predictable">Keep test data predictable</h2>

<p>Contract tests need stable data. Use seeded fixtures, factories, or isolated test tenants.</p>

<p>Avoid tests that depend on:</p>

<ul>
  <li>Production data</li>
  <li>The current date without control</li>
  <li>Test order</li>
  <li>External services that are not part of the contract</li>
  <li>Random ids that are asserted exactly</li>
</ul>

<p>Predictable data makes contract failures meaningful instead of flaky.</p>

<h2 id="start-small">Start small</h2>

<p>You do not need to contract-test every endpoint on day one.</p>

<p>Start with:</p>

<ol>
  <li>Authentication endpoints</li>
  <li>The most-used read endpoints</li>
  <li>Critical create/update flows</li>
  <li>Shared error response formats</li>
  <li>Pagination wrappers</li>
</ol>

<p>Add more coverage when endpoints become public, heavily used, or risky to change.</p>

<h2 id="closing-thoughts">Closing thoughts</h2>

<p>Contract testing keeps API providers and consumers honest. It turns the API agreement into something that can fail in CI instead of surprising users after deployment.</p>

<p>Begin with OpenAPI validation or executable examples, then add consumer expectations for the clients that matter most. The payoff is confidence that your REST API still says what clients expect it to say.</p>]]></content><author><name>Sean</name></author><category term="rest-api" /><category term="testing" /><category term="contracts" /><summary type="html"><![CDATA[REST APIs fail when providers and consumers disagree about the contract. Maybe a field is renamed, a status code changes, or an error response stops matching the documentation. Unit tests can pass while real clients break.]]></summary><media:thumbnail xmlns:media="http://search.yahoo.com/mrss/" url="http://localhost:4001/assets/images/contract-testing-for-rest-apis.png" /><media:content medium="image" url="http://localhost:4001/assets/images/contract-testing-for-rest-apis.png" xmlns:media="http://search.yahoo.com/mrss/" /></entry><entry><title type="html">Migrating from JavaScript to Typescript</title><link href="http://localhost:4001/javascript/typescript/migration/2026/04/19/migrating-from-javascript-to-typescript.html" rel="alternate" type="text/html" title="Migrating from JavaScript to Typescript" /><published>2026-04-19T08:00:00-04:00</published><updated>2026-04-19T08:00:00-04:00</updated><id>http://localhost:4001/javascript/typescript/migration/2026/04/19/migrating-from-javascript-to-typescript</id><content type="html" xml:base="http://localhost:4001/javascript/typescript/migration/2026/04/19/migrating-from-javascript-to-typescript.html"><![CDATA[<p>Most JavaScript codebases do not need a big-bang rewrite to adopt TypeScript. The migration that works best is incremental: rename a few files, tighten compiler settings over time, and let types catch bugs at the boundaries where they matter most.</p>

<p>This guide walks through a practical path from a running JavaScript project to a TypeScript codebase your team can maintain with confidence.</p>

<h2 id="why-migrate">Why migrate</h2>

<p>TypeScript does not replace JavaScript — it adds a static type layer on top of it. That layer pays off when:</p>

<ul>
  <li><strong>Interfaces change often</strong> — types document contracts and break builds when callers drift</li>
  <li><strong>Teams grow</strong> — autocomplete and inline docs reduce onboarding time</li>
  <li><strong>Refactors are risky</strong> — the compiler finds renamed fields and missing arguments before runtime</li>
  <li><strong>API boundaries are messy</strong> — request payloads, database rows, and config objects benefit from explicit shapes</li>
</ul>

<p>The goal is not 100% type coverage on day one. The goal is fewer production bugs and safer refactors with minimal workflow disruption.</p>

<h2 id="prepare-the-codebase">Prepare the codebase</h2>

<p>Before touching file extensions, make the project easy to compile:</p>

<ol>
  <li><strong>Confirm your build pipeline</strong> — webpack, Vite, esbuild, and <code class="language-plaintext highlighter-rouge">tsc</code> all support TypeScript; pick one source of truth for transpilation</li>
  <li><strong>Add a baseline <code class="language-plaintext highlighter-rouge">tsconfig.json</code></strong> — start permissive so existing code still passes</li>
  <li><strong>Run your test suite</strong> — migration should not change runtime behavior; tests are your safety net</li>
  <li><strong>Fix obvious lint issues</strong> — unused variables and implicit globals become harder to ignore once TypeScript is enabled</li>
</ol>

<p>A starter config for a gradual migration often looks like this:</p>

<div class="language-json highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="p">{</span><span class="w">
  </span><span class="nl">"compilerOptions"</span><span class="p">:</span><span class="w"> </span><span class="p">{</span><span class="w">
    </span><span class="nl">"target"</span><span class="p">:</span><span class="w"> </span><span class="s2">"ES2020"</span><span class="p">,</span><span class="w">
    </span><span class="nl">"module"</span><span class="p">:</span><span class="w"> </span><span class="s2">"ESNext"</span><span class="p">,</span><span class="w">
    </span><span class="nl">"moduleResolution"</span><span class="p">:</span><span class="w"> </span><span class="s2">"bundler"</span><span class="p">,</span><span class="w">
    </span><span class="nl">"allowJs"</span><span class="p">:</span><span class="w"> </span><span class="kc">true</span><span class="p">,</span><span class="w">
    </span><span class="nl">"checkJs"</span><span class="p">:</span><span class="w"> </span><span class="kc">false</span><span class="p">,</span><span class="w">
    </span><span class="nl">"strict"</span><span class="p">:</span><span class="w"> </span><span class="kc">false</span><span class="p">,</span><span class="w">
    </span><span class="nl">"skipLibCheck"</span><span class="p">:</span><span class="w"> </span><span class="kc">true</span><span class="p">,</span><span class="w">
    </span><span class="nl">"esModuleInterop"</span><span class="p">:</span><span class="w"> </span><span class="kc">true</span><span class="p">,</span><span class="w">
    </span><span class="nl">"outDir"</span><span class="p">:</span><span class="w"> </span><span class="s2">"dist"</span><span class="p">,</span><span class="w">
    </span><span class="nl">"rootDir"</span><span class="p">:</span><span class="w"> </span><span class="s2">"src"</span><span class="w">
  </span><span class="p">},</span><span class="w">
  </span><span class="nl">"include"</span><span class="p">:</span><span class="w"> </span><span class="p">[</span><span class="s2">"src/**/*"</span><span class="p">]</span><span class="w">
</span><span class="p">}</span><span class="w">
</span></code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">allowJs: true</code> lets <code class="language-plaintext highlighter-rouge">.js</code> and <code class="language-plaintext highlighter-rouge">.ts</code> files coexist while you migrate file by file.</p>

<h2 id="enable-typescript-gradually">Enable TypeScript gradually</h2>

<p>Avoid renaming every file at once. A proven order:</p>

<ol>
  <li>Add TypeScript and a <code class="language-plaintext highlighter-rouge">tsconfig.json</code></li>
  <li>Rename leaf modules first — utilities, helpers, and pure functions with few dependencies</li>
  <li>Move up the dependency graph toward entry points and framework wiring</li>
  <li>Rename <code class="language-plaintext highlighter-rouge">.js</code> to <code class="language-plaintext highlighter-rouge">.ts</code> (or <code class="language-plaintext highlighter-rouge">.tsx</code> for React) only when you are ready to fix type errors in that file</li>
</ol>

<p>Each renamed file should compile cleanly before you move on. Small PRs keep review manageable and reduce merge conflicts.</p>

<h2 id="type-boundaries-first">Type boundaries first</h2>

<p>Not all code needs rich types immediately. Prioritize surfaces where bad data causes the most damage:</p>

<ul>
  <li><strong>HTTP handlers</strong> — request bodies, query params, and response payloads</li>
  <li><strong>Database access</strong> — row shapes returned from queries or ORMs</li>
  <li><strong>Configuration</strong> — environment variables and feature flags</li>
  <li><strong>Shared libraries</strong> — functions exported to other packages or teams</li>
</ul>

<p>Example: a REST handler that accepts JSON benefits from an explicit input type:</p>

<div class="language-typescript highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kd">type</span> <span class="nx">CreateUserInput</span> <span class="o">=</span> <span class="p">{</span>
  <span class="na">email</span><span class="p">:</span> <span class="kr">string</span><span class="p">;</span>
  <span class="nl">name</span><span class="p">:</span> <span class="kr">string</span><span class="p">;</span>
<span class="p">};</span>

<span class="kd">function</span> <span class="nf">parseCreateUser</span><span class="p">(</span><span class="nx">body</span><span class="p">:</span> <span class="nx">unknown</span><span class="p">):</span> <span class="nx">CreateUserInput</span> <span class="p">{</span>
  <span class="k">if </span><span class="p">(</span>
    <span class="k">typeof</span> <span class="nx">body</span> <span class="o">!==</span> <span class="dl">"</span><span class="s2">object</span><span class="dl">"</span> <span class="o">||</span>
    <span class="nx">body</span> <span class="o">===</span> <span class="kc">null</span> <span class="o">||</span>
    <span class="k">typeof </span><span class="p">(</span><span class="nx">body</span> <span class="kd">as </span><span class="nx">CreateUserInput</span><span class="p">).</span><span class="nx">email</span> <span class="o">!==</span> <span class="dl">"</span><span class="s2">string</span><span class="dl">"</span> <span class="o">||</span>
    <span class="k">typeof </span><span class="p">(</span><span class="nx">body</span> <span class="kd">as </span><span class="nx">CreateUserInput</span><span class="p">).</span><span class="nx">name</span> <span class="o">!==</span> <span class="dl">"</span><span class="s2">string</span><span class="dl">"</span>
  <span class="p">)</span> <span class="p">{</span>
    <span class="k">throw</span> <span class="k">new</span> <span class="nc">Error</span><span class="p">(</span><span class="dl">"</span><span class="s2">Invalid request body</span><span class="dl">"</span><span class="p">);</span>
  <span class="p">}</span>
  <span class="k">return</span> <span class="nx">body</span> <span class="kd">as </span><span class="nx">CreateUserInput</span><span class="p">;</span>
<span class="p">}</span>
</code></pre></div></div>

<p>Once boundaries are typed, internal implementation details can stay loosely typed until you refactor them.</p>

<h2 id="handle-third-party-packages">Handle third-party packages</h2>

<p>Most popular npm packages ship TypeScript definitions. Install <code class="language-plaintext highlighter-rouge">@types/*</code> packages when types are maintained separately:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>pnpm add <span class="nt">-D</span> typescript @types/node
</code></pre></div></div>

<p>When a library has no types:</p>

<ul>
  <li>Check for a DefinitelyTyped package (<code class="language-plaintext highlighter-rouge">@types/lodash</code>, etc.)</li>
  <li>Write a minimal <code class="language-plaintext highlighter-rouge">.d.ts</code> module declaration for the parts you use</li>
  <li>Wrap untyped libraries behind your own typed adapter so <code class="language-plaintext highlighter-rouge">@ts-ignore</code> does not spread through the codebase</li>
</ul>

<p><code class="language-plaintext highlighter-rouge">skipLibCheck: true</code> avoids type errors inside node_modules while you focus on your own code.</p>

<h2 id="tighten-compiler-settings-over-time">Tighten compiler settings over time</h2>

<p>Start permissive, then enable stricter flags as coverage improves:</p>

<table>
  <thead>
    <tr>
      <th>Setting</th>
      <th>What it catches</th>
      <th>When to enable</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">noImplicitAny</code></td>
      <td>Functions and variables inferred as <code class="language-plaintext highlighter-rouge">any</code></td>
      <td>After most files are <code class="language-plaintext highlighter-rouge">.ts</code></td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">strictNullChecks</code></td>
      <td><code class="language-plaintext highlighter-rouge">null</code> and <code class="language-plaintext highlighter-rouge">undefined</code> misuse</td>
      <td>When core models are typed</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">strict</code></td>
      <td>Full strict mode bundle</td>
      <td>When the team is comfortable fixing edge cases</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">noUncheckedIndexedAccess</code></td>
      <td>Unsafe array/object index access</td>
      <td>For data-heavy code paths</td>
    </tr>
  </tbody>
</table>

<p>Turn on one flag at a time, fix the resulting errors in a dedicated PR, and document the new standard for the team.</p>

<h2 id="common-pitfalls">Common pitfalls</h2>

<ul>
  <li><strong>Chasing 100% coverage too early</strong> — <code class="language-plaintext highlighter-rouge">any</code> in a legacy module is acceptable during migration; isolate it and schedule cleanup</li>
  <li><strong>Over-typing before understanding runtime behavior</strong> — types should reflect what the code actually does, not an idealized API</li>
  <li><strong>Mixing build tools</strong> — let one tool own transpilation; avoid running Babel and <code class="language-plaintext highlighter-rouge">tsc</code> with conflicting settings</li>
  <li><strong>Ignoring test types</strong> — add <code class="language-plaintext highlighter-rouge">@types/jest</code> or <code class="language-plaintext highlighter-rouge">@types/mocha</code> so test helpers are typed too</li>
  <li><strong>Big-bang PRs</strong> — large migrations stall in review; incremental renames merge faster</li>
</ul>

<h2 id="a-practical-migration-checklist">A practical migration checklist</h2>

<ul class="task-list">
  <li class="task-list-item"><input type="checkbox" class="task-list-item-checkbox" disabled="disabled" /><code class="language-plaintext highlighter-rouge">tsconfig.json</code> with <code class="language-plaintext highlighter-rouge">allowJs: true</code></li>
  <li class="task-list-item"><input type="checkbox" class="task-list-item-checkbox" disabled="disabled" />TypeScript added as a dev dependency</li>
  <li class="task-list-item"><input type="checkbox" class="task-list-item-checkbox" disabled="disabled" />Build and test scripts updated</li>
  <li class="task-list-item"><input type="checkbox" class="task-list-item-checkbox" disabled="disabled" />Boundary modules typed (API, config, persistence)</li>
  <li class="task-list-item"><input type="checkbox" class="task-list-item-checkbox" disabled="disabled" />Third-party types installed or stubbed</li>
  <li class="task-list-item"><input type="checkbox" class="task-list-item-checkbox" disabled="disabled" />Stricter compiler flags enabled incrementally</li>
  <li class="task-list-item"><input type="checkbox" class="task-list-item-checkbox" disabled="disabled" />Team agrees on conventions for <code class="language-plaintext highlighter-rouge">any</code>, assertions, and new code</li>
</ul>

<h2 id="closing-thoughts">Closing thoughts</h2>

<p>Migrating from JavaScript to TypeScript is a process, not a switch. Rename files in dependency order, type the edges where bugs are expensive, and tighten compiler settings as confidence grows.</p>

<p>Done well, the migration feels invisible to users — same runtime behavior, fewer surprises for developers. Start with one module this week, ship it, and expand from there.</p>]]></content><author><name>Sean</name></author><category term="javascript" /><category term="typescript" /><category term="migration" /><summary type="html"><![CDATA[Most JavaScript codebases do not need a big-bang rewrite to adopt TypeScript. The migration that works best is incremental: rename a few files, tighten compiler settings over time, and let types catch bugs at the boundaries where they matter most.]]></summary><media:thumbnail xmlns:media="http://search.yahoo.com/mrss/" url="http://localhost:4001/assets/images/migrating-from-javascript-to-typescript.png" /><media:content medium="image" url="http://localhost:4001/assets/images/migrating-from-javascript-to-typescript.png" xmlns:media="http://search.yahoo.com/mrss/" /></entry><entry><title type="html">Testing REST APIs from the Command Line</title><link href="http://localhost:4001/rest-api/testing/cli/2026/03/12/testing-rest-apis-from-the-command-line.html" rel="alternate" type="text/html" title="Testing REST APIs from the Command Line" /><published>2026-03-12T15:25:00-04:00</published><updated>2026-03-12T15:25:00-04:00</updated><id>http://localhost:4001/rest-api/testing/cli/2026/03/12/testing-rest-apis-from-the-command-line</id><content type="html" xml:base="http://localhost:4001/rest-api/testing/cli/2026/03/12/testing-rest-apis-from-the-command-line.html"><![CDATA[<p>You do not need a full API client to test every endpoint. The command line is fast, scriptable, and available almost everywhere. For quick checks, CI smoke tests, and debugging from a server, tools like <code class="language-plaintext highlighter-rouge">curl</code>, <code class="language-plaintext highlighter-rouge">jq</code>, and HTTPie are hard to beat.</p>

<p>This guide focuses on practical REST API testing from a terminal.</p>

<h2 id="start-with-curl">Start with curl</h2>

<p>The simplest request is a <code class="language-plaintext highlighter-rouge">GET</code>:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>curl https://api.example.com/health
</code></pre></div></div>

<p>Add <code class="language-plaintext highlighter-rouge">-i</code> when you want response headers:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>curl <span class="nt">-i</span> https://api.example.com/health
</code></pre></div></div>

<p>For JSON APIs, include an <code class="language-plaintext highlighter-rouge">Accept</code> header:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>curl <span class="nt">-i</span> <span class="se">\</span>
  <span class="nt">-H</span> <span class="s2">"Accept: application/json"</span> <span class="se">\</span>
  https://api.example.com/users/123
</code></pre></div></div>

<p>Headers often explain more than the body: content type, cache policy, rate limits, request ids, and auth challenges all live there.</p>

<h2 id="send-json-bodies">Send JSON bodies</h2>

<p>For <code class="language-plaintext highlighter-rouge">POST</code>, <code class="language-plaintext highlighter-rouge">PUT</code>, and <code class="language-plaintext highlighter-rouge">PATCH</code>, send JSON with <code class="language-plaintext highlighter-rouge">-d</code> and a content type:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>curl <span class="nt">-i</span> <span class="se">\</span>
  <span class="nt">-X</span> POST <span class="se">\</span>
  <span class="nt">-H</span> <span class="s2">"Content-Type: application/json"</span> <span class="se">\</span>
  <span class="nt">-H</span> <span class="s2">"Accept: application/json"</span> <span class="se">\</span>
  <span class="nt">-d</span> <span class="s1">'{"email":"avery@example.com","name":"Avery"}'</span> <span class="se">\</span>
  https://api.example.com/users
</code></pre></div></div>

<p>When the body is larger, put it in a file:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>curl <span class="nt">-i</span> <span class="se">\</span>
  <span class="nt">-X</span> POST <span class="se">\</span>
  <span class="nt">-H</span> <span class="s2">"Content-Type: application/json"</span> <span class="se">\</span>
  <span class="nt">-d</span> @create-user.json <span class="se">\</span>
  https://api.example.com/users
</code></pre></div></div>

<p>Files make requests easier to review and reuse.</p>

<h2 id="add-authentication">Add authentication</h2>

<p>Bearer tokens:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>curl <span class="nt">-i</span> <span class="se">\</span>
  <span class="nt">-H</span> <span class="s2">"Authorization: Bearer </span><span class="nv">$ACCESS_TOKEN</span><span class="s2">"</span> <span class="se">\</span>
  https://api.example.com/users/me
</code></pre></div></div>

<p>API keys:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>curl <span class="nt">-i</span> <span class="se">\</span>
  <span class="nt">-H</span> <span class="s2">"X-API-Key: </span><span class="nv">$API_KEY</span><span class="s2">"</span> <span class="se">\</span>
  https://api.example.com/reports
</code></pre></div></div>

<p>Use environment variables for local testing, but avoid printing secrets in shared logs or shell history.</p>

<h2 id="format-json-with-jq">Format JSON with jq</h2>

<p><code class="language-plaintext highlighter-rouge">jq</code> makes JSON responses readable and testable.</p>

<p>Pretty-print:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>curl <span class="nt">-s</span> https://api.example.com/users/123 | jq <span class="nb">.</span>
</code></pre></div></div>

<p>Extract a field:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>curl <span class="nt">-s</span> https://api.example.com/users/123 | jq <span class="nt">-r</span> <span class="s1">'.email'</span>
</code></pre></div></div>

<p>Check a value:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>curl <span class="nt">-s</span> https://api.example.com/users/123 | jq <span class="nt">-e</span> <span class="s1">'.id == "usr_123"'</span>
</code></pre></div></div>

<p>The <code class="language-plaintext highlighter-rouge">-e</code> flag makes <code class="language-plaintext highlighter-rouge">jq</code> exit with success or failure, which is useful in scripts and CI.</p>

<h2 id="capture-status-codes">Capture status codes</h2>

<p>Sometimes you need the HTTP status code separately from the body:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>curl <span class="nt">-s</span> <span class="nt">-o</span> response.json <span class="nt">-w</span> <span class="s2">"%{http_code}</span><span class="se">\n</span><span class="s2">"</span> <span class="se">\</span>
  https://api.example.com/users/123
</code></pre></div></div>

<p>That writes the body to <code class="language-plaintext highlighter-rouge">response.json</code> and prints the status code.</p>

<p>A shell test can fail on unexpected status:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nv">status</span><span class="o">=</span><span class="si">$(</span>curl <span class="nt">-s</span> <span class="nt">-o</span> response.json <span class="nt">-w</span> <span class="s2">"%{http_code}"</span> https://api.example.com/health<span class="si">)</span>
<span class="nb">test</span> <span class="s2">"</span><span class="nv">$status</span><span class="s2">"</span> <span class="o">=</span> <span class="s2">"200"</span>
</code></pre></div></div>

<p>Keep scripts small and obvious. If a test grows complex, it may belong in a real test framework.</p>

<h2 id="use-httpie-for-readable-commands">Use HTTPie for readable commands</h2>

<p>HTTPie provides a friendlier syntax for many manual tests:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>http GET https://api.example.com/users/123 <span class="se">\</span>
  Authorization:<span class="s2">"Bearer </span><span class="nv">$ACCESS_TOKEN</span><span class="s2">"</span>
</code></pre></div></div>

<p>Posting JSON:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>http POST https://api.example.com/users <span class="se">\</span>
  <span class="nv">email</span><span class="o">=</span>avery@example.com <span class="se">\</span>
  <span class="nv">name</span><span class="o">=</span>Avery
</code></pre></div></div>

<p>HTTPie is especially nice for exploratory testing. <code class="language-plaintext highlighter-rouge">curl</code> is still more universal for scripts because it is installed in more environments.</p>

<h2 id="test-error-cases">Test error cases</h2>

<p>Do not only test the happy path. From the command line, it is easy to send bad input:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>curl <span class="nt">-i</span> <span class="se">\</span>
  <span class="nt">-X</span> POST <span class="se">\</span>
  <span class="nt">-H</span> <span class="s2">"Content-Type: application/json"</span> <span class="se">\</span>
  <span class="nt">-d</span> <span class="s1">'{"email":"not-an-email"}'</span> <span class="se">\</span>
  https://api.example.com/users
</code></pre></div></div>

<p>Check that the API returns:</p>

<ul>
  <li>A useful <code class="language-plaintext highlighter-rouge">4xx</code> status</li>
  <li>A stable error code</li>
  <li>Field-level validation details</li>
  <li>No stack traces or internal secrets</li>
</ul>

<p>These checks catch many API quality issues early.</p>

<h2 id="build-a-small-smoke-test">Build a small smoke test</h2>

<p>A simple smoke test might verify health, auth, and one read endpoint:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c">#!/usr/bin/env bash</span>
<span class="nb">set</span> <span class="nt">-euo</span> pipefail

<span class="nv">base_url</span><span class="o">=</span><span class="s2">"</span><span class="k">${</span><span class="nv">BASE_URL</span><span class="k">:-</span><span class="nv">https</span>://api.example.com<span class="k">}</span><span class="s2">"</span>

<span class="nv">status</span><span class="o">=</span><span class="si">$(</span>curl <span class="nt">-s</span> <span class="nt">-o</span> /tmp/health.json <span class="nt">-w</span> <span class="s2">"%{http_code}"</span> <span class="s2">"</span><span class="nv">$base_url</span><span class="s2">/health"</span><span class="si">)</span>
<span class="nb">test</span> <span class="s2">"</span><span class="nv">$status</span><span class="s2">"</span> <span class="o">=</span> <span class="s2">"200"</span>

curl <span class="nt">-s</span> <span class="se">\</span>
  <span class="nt">-H</span> <span class="s2">"Authorization: Bearer </span><span class="nv">$ACCESS_TOKEN</span><span class="s2">"</span> <span class="se">\</span>
  <span class="s2">"</span><span class="nv">$base_url</span><span class="s2">/users/me"</span> | jq <span class="nt">-e</span> <span class="s1">'.id and .email'</span> <span class="o">&gt;</span> /dev/null
</code></pre></div></div>

<p>This is not a full test suite, but it is a useful deployment check.</p>

<h2 id="closing-thoughts">Closing thoughts</h2>

<p>Command-line API testing is fast because it removes ceremony. You can send a request, inspect headers, pipe JSON into checks, and save the command in documentation or CI.</p>

<p>Use <code class="language-plaintext highlighter-rouge">curl</code> for portability, <code class="language-plaintext highlighter-rouge">jq</code> for JSON assertions, and HTTPie when readability matters. Together, they cover a surprising amount of day-to-day REST API testing.</p>]]></content><author><name>Sean</name></author><category term="rest-api" /><category term="testing" /><category term="cli" /><summary type="html"><![CDATA[You do not need a full API client to test every endpoint. The command line is fast, scriptable, and available almost everywhere. For quick checks, CI smoke tests, and debugging from a server, tools like curl, jq, and HTTPie are hard to beat.]]></summary><media:thumbnail xmlns:media="http://search.yahoo.com/mrss/" url="http://localhost:4001/assets/images/testing-rest-apis-from-the-command-line.png" /><media:content medium="image" url="http://localhost:4001/assets/images/testing-rest-apis-from-the-command-line.png" xmlns:media="http://search.yahoo.com/mrss/" /></entry><entry><title type="html">HTTP Caching for REST APIs</title><link href="http://localhost:4001/rest-api/http/caching/2026/02/07/http-caching-for-rest-apis.html" rel="alternate" type="text/html" title="HTTP Caching for REST APIs" /><published>2026-02-07T05:40:00-05:00</published><updated>2026-02-07T05:40:00-05:00</updated><id>http://localhost:4001/rest-api/http/caching/2026/02/07/http-caching-for-rest-apis</id><content type="html" xml:base="http://localhost:4001/rest-api/http/caching/2026/02/07/http-caching-for-rest-apis.html"><![CDATA[<p>Caching is one of the most effective ways to make REST APIs faster, but it is also easy to get wrong. A stale public profile is annoying. A stale billing response is a serious bug.</p>

<p>HTTP already gives APIs a useful caching vocabulary. The trick is choosing the right headers for each kind of response.</p>

<h2 id="start-with-cache-control">Start with Cache-Control</h2>

<p><code class="language-plaintext highlighter-rouge">Cache-Control</code> tells browsers, proxies, and CDNs how a response may be cached.</p>

<p>Example:</p>

<div class="language-http highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="err">Cache-Control: public, max-age=300
</span></code></pre></div></div>

<p>This means the response may be stored by shared caches and reused for 300 seconds.</p>

<p>Common directives:</p>

<table>
  <thead>
    <tr>
      <th>Directive</th>
      <th>Meaning</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">public</code></td>
      <td>Shared caches may store the response</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">private</code></td>
      <td>Only the end user’s browser may store it</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">no-store</code></td>
      <td>Do not store the response at all</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">no-cache</code></td>
      <td>Store it, but revalidate before reuse</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">max-age=60</code></td>
      <td>Fresh for 60 seconds</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">s-maxage=300</code></td>
      <td>Fresh for shared caches for 300 seconds</td>
    </tr>
  </tbody>
</table>

<p>Use <code class="language-plaintext highlighter-rouge">no-store</code> for sensitive responses such as tokens, account secrets, or payment details.</p>

<h2 id="cache-public-stable-reads">Cache public, stable reads</h2>

<p>Good candidates for caching:</p>

<ul>
  <li>Public documentation metadata</li>
  <li>Product catalogs</li>
  <li>Feature flag definitions that change slowly</li>
  <li>Public user profiles</li>
  <li>Static lookup tables</li>
</ul>

<p>Example:</p>

<div class="language-http highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="err">GET /countries
Cache-Control: public, max-age=86400
</span></code></pre></div></div>

<p>If the data changes rarely, a long cache lifetime can remove a lot of repeated traffic.</p>

<h2 id="keep-personalized-responses-private">Keep personalized responses private</h2>

<p>Responses tied to one user should not be stored by shared caches unless you are very sure the cache key includes the user identity.</p>

<p>Safer default:</p>

<div class="language-http highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="err">Cache-Control: private, max-age=60
</span></code></pre></div></div>

<p>For highly sensitive data:</p>

<div class="language-http highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="err">Cache-Control: no-store
</span></code></pre></div></div>

<p>Be especially careful with APIs behind CDNs. A missing <code class="language-plaintext highlighter-rouge">private</code> or <code class="language-plaintext highlighter-rouge">no-store</code> directive can accidentally cache user-specific data in a shared layer.</p>

<h2 id="use-etags-for-validation">Use ETags for validation</h2>

<p>An <code class="language-plaintext highlighter-rouge">ETag</code> is a version identifier for a response representation.</p>

<p>First response:</p>

<div class="language-http highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">HTTP</span><span class="o">/</span><span class="m">1.1</span> <span class="m">200</span> <span class="ne">OK</span>
<span class="na">ETag</span><span class="p">:</span> <span class="s">"users-123-v7"</span>
<span class="na">Cache-Control</span><span class="p">:</span> <span class="s">private, no-cache</span>
</code></pre></div></div>

<p>Later request:</p>

<div class="language-http highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="err">GET /users/123
If-None-Match: "users-123-v7"
</span></code></pre></div></div>

<p>If nothing changed, the server can return:</p>

<div class="language-http highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">HTTP</span><span class="o">/</span><span class="m">1.1</span> <span class="m">304</span> <span class="ne">Not Modified</span>
</code></pre></div></div>

<p>The client reuses its cached body, and the API avoids sending the same JSON again.</p>

<h2 id="last-modified-also-works">Last-Modified also works</h2>

<p><code class="language-plaintext highlighter-rouge">Last-Modified</code> and <code class="language-plaintext highlighter-rouge">If-Modified-Since</code> are timestamp-based validation headers.</p>

<div class="language-http highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="err">Last-Modified: Sat, 07 Feb 2026 10:40:00 GMT
</span></code></pre></div></div>

<p>They are easy to implement when resources already have an <code class="language-plaintext highlighter-rouge">updated_at</code> field. ETags are usually more precise because they can represent any change in the response, not only timestamp changes.</p>

<h2 id="handle-unsafe-methods-carefully">Handle unsafe methods carefully</h2>

<p><code class="language-plaintext highlighter-rouge">GET</code> and <code class="language-plaintext highlighter-rouge">HEAD</code> are the usual cacheable methods. Mutating requests such as <code class="language-plaintext highlighter-rouge">POST</code>, <code class="language-plaintext highlighter-rouge">PATCH</code>, and <code class="language-plaintext highlighter-rouge">DELETE</code> should generally not be cached.</p>

<p>After a mutation, clients may need to invalidate or refetch cached reads:</p>

<div class="language-http highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="err">PATCH /users/123
GET /users/123
</span></code></pre></div></div>

<p>If you control both API and client, document which reads are affected by writes. If you use a CDN, make sure your purge or surrogate key strategy matches your resource model.</p>

<h2 id="vary-when-responses-depend-on-headers">Vary when responses depend on headers</h2>

<p>The <code class="language-plaintext highlighter-rouge">Vary</code> header tells caches which request headers affect the response.</p>

<p>Example:</p>

<div class="language-http highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="err">Vary: Accept-Encoding, Accept-Language
</span></code></pre></div></div>

<p>If an API response differs by authorization or tenant headers, shared caching becomes more complicated. In many cases, <code class="language-plaintext highlighter-rouge">private</code> or <code class="language-plaintext highlighter-rouge">no-store</code> is safer than trying to build a perfect shared cache key for personalized data.</p>

<h2 id="avoid-caching-errors-blindly">Avoid caching errors blindly</h2>

<p>Some error responses can be cached. For example, a public <code class="language-plaintext highlighter-rouge">404</code> for a missing static resource may be fine.</p>

<p>But be careful with:</p>

<ul>
  <li><code class="language-plaintext highlighter-rouge">401</code> and <code class="language-plaintext highlighter-rouge">403</code></li>
  <li>temporary <code class="language-plaintext highlighter-rouge">500</code> errors</li>
  <li>rate-limit responses</li>
  <li>validation errors tied to one request</li>
</ul>

<p>If you do cache errors, make the lifetime short and intentional.</p>

<h2 id="a-practical-caching-checklist">A practical caching checklist</h2>

<p>For each <code class="language-plaintext highlighter-rouge">GET</code> endpoint, decide:</p>

<ul class="task-list">
  <li class="task-list-item"><input type="checkbox" class="task-list-item-checkbox" disabled="disabled" />Is the response public, private, or sensitive?</li>
  <li class="task-list-item"><input type="checkbox" class="task-list-item-checkbox" disabled="disabled" />Can shared caches store it?</li>
  <li class="task-list-item"><input type="checkbox" class="task-list-item-checkbox" disabled="disabled" />How long is it fresh?</li>
  <li class="task-list-item"><input type="checkbox" class="task-list-item-checkbox" disabled="disabled" />Should clients revalidate with <code class="language-plaintext highlighter-rouge">ETag</code> or <code class="language-plaintext highlighter-rouge">Last-Modified</code>?</li>
  <li class="task-list-item"><input type="checkbox" class="task-list-item-checkbox" disabled="disabled" />Does the response vary by headers?</li>
  <li class="task-list-item"><input type="checkbox" class="task-list-item-checkbox" disabled="disabled" />How are caches invalidated after writes?</li>
  <li class="task-list-item"><input type="checkbox" class="task-list-item-checkbox" disabled="disabled" />Are error responses cacheable?</li>
</ul>

<h2 id="closing-thoughts">Closing thoughts</h2>

<p>HTTP caching works best when each endpoint has a clear freshness policy. Do not rely on defaults, and do not treat all responses the same.</p>

<p>Cache public, stable data aggressively. Revalidate data that changes. Keep sensitive or personalized responses private or unstored. Those choices can improve performance without turning stale data into a production incident.</p>]]></content><author><name>Sean</name></author><category term="rest-api" /><category term="http" /><category term="caching" /><summary type="html"><![CDATA[Caching is one of the most effective ways to make REST APIs faster, but it is also easy to get wrong. A stale public profile is annoying. A stale billing response is a serious bug.]]></summary><media:thumbnail xmlns:media="http://search.yahoo.com/mrss/" url="http://localhost:4001/assets/images/http-caching-for-rest-apis.png" /><media:content medium="image" url="http://localhost:4001/assets/images/http-caching-for-rest-apis.png" xmlns:media="http://search.yahoo.com/mrss/" /></entry><entry><title type="html">Writing REST APIs that Developers Will Love</title><link href="http://localhost:4001/rest-api/design/2026/01/21/writing-rest-apis-that-developers-will-love.html" rel="alternate" type="text/html" title="Writing REST APIs that Developers Will Love" /><published>2026-01-21T07:00:00-05:00</published><updated>2026-01-21T07:00:00-05:00</updated><id>http://localhost:4001/rest-api/design/2026/01/21/writing-rest-apis-that-developers-will-love</id><content type="html" xml:base="http://localhost:4001/rest-api/design/2026/01/21/writing-rest-apis-that-developers-will-love.html"><![CDATA[<p>Developers judge an API quickly. The first request either feels obvious or it does not. A URL that makes sense, a response that matches expectations, and an error message that actually helps — those details add up to an API people want to use.</p>

<p>This guide covers practical design choices that make REST APIs easier to learn, harder to misuse, and more pleasant to integrate with.</p>

<h2 id="start-with-resources-not-actions">Start with resources, not actions</h2>

<p>REST APIs work best when URLs represent <strong>nouns</strong> (resources), not verbs (operations).</p>

<p>Good:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>GET    /users/42
POST   /users
PATCH  /users/42
DELETE /users/42
</code></pre></div></div>

<p>Avoid:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>POST /getUser
POST /createUser
POST /deleteUserById
</code></pre></div></div>

<p>Use HTTP methods to express intent. <code class="language-plaintext highlighter-rouge">GET</code> reads, <code class="language-plaintext highlighter-rouge">POST</code> creates, <code class="language-plaintext highlighter-rouge">PUT</code> replaces, <code class="language-plaintext highlighter-rouge">PATCH</code> updates partially, and <code class="language-plaintext highlighter-rouge">DELETE</code> removes. When every endpoint is a <code class="language-plaintext highlighter-rouge">POST</code> with a different action name, clients lose the predictability that makes REST worth using.</p>

<p>Nested resources are fine when the relationship is clear:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>GET /users/42/posts
GET /users/42/posts/7
</code></pre></div></div>

<p>Keep nesting shallow. If a path grows past two levels, consider flattening:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>GET /posts?user_id=42
</code></pre></div></div>

<h2 id="use-http-status-codes-consistently">Use HTTP status codes consistently</h2>

<p>Status codes communicate outcome before the client parses the body. Use them consistently across the API:</p>

<table>
  <thead>
    <tr>
      <th>Code</th>
      <th>When to use</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">200 OK</code></td>
      <td>Successful read or update with a response body</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">201 Created</code></td>
      <td>Resource created; include a <code class="language-plaintext highlighter-rouge">Location</code> header when possible</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">204 No Content</code></td>
      <td>Successful delete or update with no body</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">400 Bad Request</code></td>
      <td>Invalid input the client can fix</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">401 Unauthorized</code></td>
      <td>Missing or invalid authentication</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">403 Forbidden</code></td>
      <td>Authenticated but not allowed</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">404 Not Found</code></td>
      <td>Resource does not exist (or is not visible to this caller)</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">409 Conflict</code></td>
      <td>Duplicate or state conflict</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">422 Unprocessable Entity</code></td>
      <td>Valid syntax but business rule failure</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">429 Too Many Requests</code></td>
      <td>Rate limit exceeded</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">500 Internal Server Error</code></td>
      <td>Unexpected server failure</td>
    </tr>
  </tbody>
</table>

<p>Do not return <code class="language-plaintext highlighter-rouge">200 OK</code> with an error payload. Clients that rely on status codes — and most HTTP libraries do — will treat the request as successful. Pick one error shape and one set of status rules, then apply them everywhere.</p>

<h2 id="design-errors-developers-can-act-on">Design errors developers can act on</h2>

<p>A useful error response answers three questions:</p>

<ol>
  <li>What went wrong?</li>
  <li>Which field or parameter caused it?</li>
  <li>What should the client do next?</li>
</ol>

<p>Example:</p>

<div class="language-json highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="p">{</span><span class="w">
  </span><span class="nl">"error"</span><span class="p">:</span><span class="w"> </span><span class="p">{</span><span class="w">
    </span><span class="nl">"code"</span><span class="p">:</span><span class="w"> </span><span class="s2">"validation_failed"</span><span class="p">,</span><span class="w">
    </span><span class="nl">"message"</span><span class="p">:</span><span class="w"> </span><span class="s2">"One or more fields are invalid."</span><span class="p">,</span><span class="w">
    </span><span class="nl">"details"</span><span class="p">:</span><span class="w"> </span><span class="p">[</span><span class="w">
      </span><span class="p">{</span><span class="w">
        </span><span class="nl">"field"</span><span class="p">:</span><span class="w"> </span><span class="s2">"email"</span><span class="p">,</span><span class="w">
        </span><span class="nl">"message"</span><span class="p">:</span><span class="w"> </span><span class="s2">"Must be a valid email address."</span><span class="w">
      </span><span class="p">}</span><span class="w">
    </span><span class="p">]</span><span class="w">
  </span><span class="p">}</span><span class="w">
</span><span class="p">}</span><span class="w">
</span></code></pre></div></div>

<p>Stable <code class="language-plaintext highlighter-rouge">code</code> values matter more than clever messages. Messages can change; codes should not. Document every error code in your API reference so integrators know what to handle programmatically.</p>

<h2 id="keep-request-and-response-shapes-predictable">Keep request and response shapes predictable</h2>

<p>Developers build mental models from patterns. Reuse them.</p>

<ul>
  <li>Use <strong>snake_case</strong> or <strong>camelCase</strong> consistently — pick one and stick with it across every endpoint</li>
  <li>Return the same resource representation from <code class="language-plaintext highlighter-rouge">GET</code>, <code class="language-plaintext highlighter-rouge">POST</code>, and <code class="language-plaintext highlighter-rouge">PATCH</code> where possible</li>
  <li>Include an <code class="language-plaintext highlighter-rouge">id</code> field on every resource</li>
  <li>Use ISO 8601 for timestamps: <code class="language-plaintext highlighter-rouge">2026-01-21T12:00:00Z</code></li>
  <li>Represent enums as strings, not opaque integers</li>
</ul>

<p>For collections, return a consistent wrapper:</p>

<div class="language-json highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="p">{</span><span class="w">
  </span><span class="nl">"data"</span><span class="p">:</span><span class="w"> </span><span class="p">[</span><span class="w">
    </span><span class="p">{</span><span class="w"> </span><span class="nl">"id"</span><span class="p">:</span><span class="w"> </span><span class="mi">1</span><span class="p">,</span><span class="w"> </span><span class="nl">"title"</span><span class="p">:</span><span class="w"> </span><span class="s2">"First post"</span><span class="w"> </span><span class="p">},</span><span class="w">
    </span><span class="p">{</span><span class="w"> </span><span class="nl">"id"</span><span class="p">:</span><span class="w"> </span><span class="mi">2</span><span class="p">,</span><span class="w"> </span><span class="nl">"title"</span><span class="p">:</span><span class="w"> </span><span class="s2">"Second post"</span><span class="w"> </span><span class="p">}</span><span class="w">
  </span><span class="p">],</span><span class="w">
  </span><span class="nl">"pagination"</span><span class="p">:</span><span class="w"> </span><span class="p">{</span><span class="w">
    </span><span class="nl">"page"</span><span class="p">:</span><span class="w"> </span><span class="mi">1</span><span class="p">,</span><span class="w">
    </span><span class="nl">"limit"</span><span class="p">:</span><span class="w"> </span><span class="mi">20</span><span class="p">,</span><span class="w">
    </span><span class="nl">"total"</span><span class="p">:</span><span class="w"> </span><span class="mi">142</span><span class="w">
  </span><span class="p">}</span><span class="w">
</span><span class="p">}</span><span class="w">
</span></code></pre></div></div>

<p>Avoid surprising clients with different field names or nesting on similar endpoints. Consistency reduces integration time more than any single clever shortcut.</p>

<h2 id="support-pagination-filtering-and-sorting">Support pagination, filtering, and sorting</h2>

<p>List endpoints that return unbounded arrays do not scale — for your database or for the client waiting on a multi-megabyte response.</p>

<p>Common query parameters:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>GET /posts?page=2&amp;limit=20
GET /posts?sort=-created_at
GET /posts?status=published&amp;author_id=42
</code></pre></div></div>

<p>Cursor-based pagination works better for large or frequently changing datasets:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>GET /posts?cursor=eyJpZCI6MTIzfQ&amp;limit=20
</code></pre></div></div>

<p>Document defaults (<code class="language-plaintext highlighter-rouge">limit=20</code>, <code class="language-plaintext highlighter-rouge">sort=created_at</code>) and maximums (<code class="language-plaintext highlighter-rouge">limit</code> capped at 100). Clients should not have to guess whether pagination is offset- or cursor-based.</p>

<h2 id="make-authentication-straightforward">Make authentication straightforward</h2>

<p>Most APIs use bearer tokens, API keys, or OAuth 2.0. Whatever you choose, make the contract obvious:</p>

<ul>
  <li>Document where credentials go (<code class="language-plaintext highlighter-rouge">Authorization: Bearer …</code> or a named header)</li>
  <li>Return <code class="language-plaintext highlighter-rouge">401</code> when credentials are missing or invalid</li>
  <li>Return <code class="language-plaintext highlighter-rouge">403</code> when credentials are valid but insufficient</li>
  <li>Avoid mixing auth schemes across endpoints without a clear reason</li>
</ul>

<p>If tokens expire, provide a refresh flow or clear instructions for obtaining a new token. Silent expiry with cryptic <code class="language-plaintext highlighter-rouge">403</code> responses is a common source of support tickets.</p>

<h2 id="version-your-api-deliberately">Version your API deliberately</h2>

<p>APIs change. Versioning gives you room to evolve without breaking existing clients.</p>

<p>Common approaches:</p>

<ul>
  <li><strong>URL prefix</strong> — <code class="language-plaintext highlighter-rouge">/v1/users</code> (simple, visible, widely understood)</li>
  <li><strong>Header</strong> — <code class="language-plaintext highlighter-rouge">Accept: application/vnd.myapp.v1+json</code> (clean URLs, harder to discover)</li>
  <li><strong>Query parameter</strong> — <code class="language-plaintext highlighter-rouge">/users?version=1</code> (works, but easy to forget)</li>
</ul>

<p>Pick one strategy and apply it consistently. When you ship breaking changes, increment the version and keep the old version running long enough for clients to migrate. Deprecation headers (<code class="language-plaintext highlighter-rouge">Deprecation</code>, <code class="language-plaintext highlighter-rouge">Sunset</code>) give integrators a timeline instead of a surprise.</p>

<h2 id="write-documentation-that-matches-the-api">Write documentation that matches the API</h2>

<p>Reference docs should show real requests and real responses — not abstract field lists alone.</p>

<p>Every endpoint page should include:</p>

<ul>
  <li>Method and path</li>
  <li>Required and optional parameters</li>
  <li>Example request with headers and body</li>
  <li>Example success response</li>
  <li>Example error responses</li>
  <li>Auth requirements</li>
</ul>

<p>OpenAPI (Swagger) specs are worth the investment. They power interactive docs, client generation, and contract tests. Keep the spec close to the code or generate it from source so it does not drift.</p>

<h2 id="provide-working-examples">Provide working examples</h2>

<p>Developers learn fastest by sending a request and getting a response. Lower the barrier:</p>

<ul>
  <li>Publish a <strong>Postman collection</strong> or equivalent for your API</li>
  <li>Include curl examples in documentation</li>
  <li>Offer a <strong>sandbox environment</strong> with test credentials</li>
  <li>Seed sandbox data so list endpoints return meaningful results on the first call</li>
</ul>

<p>An API with a five-minute quickstart beats one that requires reading thirty pages before the first successful request.</p>

<h2 id="design-for-backwards-compatibility">Design for backwards compatibility</h2>

<p>Breaking changes erode trust. Prefer additive changes:</p>

<ul>
  <li>Add new optional fields instead of renaming existing ones</li>
  <li>Add new endpoints instead of changing semantics on old ones</li>
  <li>Default new behavior off or preserve old behavior when possible</li>
</ul>

<p>When a change is unavoidable, communicate early:</p>

<ol>
  <li>Mark the old behavior as deprecated</li>
  <li>Document the replacement</li>
  <li>Set a sunset date</li>
  <li>Monitor usage of deprecated endpoints before removal</li>
</ol>

<p>Clients forgive evolution. They do not forgive silent breakage.</p>

<h2 id="a-practical-design-checklist">A practical design checklist</h2>

<p>Before shipping an endpoint, run through this list:</p>

<ul class="task-list">
  <li class="task-list-item"><input type="checkbox" class="task-list-item-checkbox" disabled="disabled" />URL represents a resource, not an action</li>
  <li class="task-list-item"><input type="checkbox" class="task-list-item-checkbox" disabled="disabled" />HTTP method matches the operation</li>
  <li class="task-list-item"><input type="checkbox" class="task-list-item-checkbox" disabled="disabled" />Status codes used consistently</li>
  <li class="task-list-item"><input type="checkbox" class="task-list-item-checkbox" disabled="disabled" />Error responses include a stable code and actionable details</li>
  <li class="task-list-item"><input type="checkbox" class="task-list-item-checkbox" disabled="disabled" />Request and response field naming matches the rest of the API</li>
  <li class="task-list-item"><input type="checkbox" class="task-list-item-checkbox" disabled="disabled" />List endpoints support pagination</li>
  <li class="task-list-item"><input type="checkbox" class="task-list-item-checkbox" disabled="disabled" />Auth requirements are documented and enforced consistently</li>
  <li class="task-list-item"><input type="checkbox" class="task-list-item-checkbox" disabled="disabled" />Examples in docs match actual behavior</li>
  <li class="task-list-item"><input type="checkbox" class="task-list-item-checkbox" disabled="disabled" />Breaking changes are versioned or deprecated with notice</li>
</ul>

<h2 id="closing-thoughts">Closing thoughts</h2>

<p>Developers do not love APIs because they are RESTful on paper. They love APIs that are <strong>predictable</strong>, <strong>consistent</strong>, and <strong>easy to debug</strong> when something goes wrong.</p>

<p>Start with clear resource URLs and honest status codes. Add structured errors, stable pagination, and documentation with real examples. Treat backwards compatibility as a feature, not an afterthought. Those choices compound — each good decision makes the next integration faster and each bad one becomes someone else’s workaround.</p>

<p>Build the API you would want to integrate with on a Friday afternoon. That is the bar worth hitting.</p>]]></content><author><name>Sean</name></author><category term="rest-api" /><category term="design" /><summary type="html"><![CDATA[Developers judge an API quickly. The first request either feels obvious or it does not. A URL that makes sense, a response that matches expectations, and an error message that actually helps — those details add up to an API people want to use.]]></summary><media:thumbnail xmlns:media="http://search.yahoo.com/mrss/" url="http://localhost:4001/assets/images/writing-rest-apis-developers-love.png" /><media:content medium="image" url="http://localhost:4001/assets/images/writing-rest-apis-developers-love.png" xmlns:media="http://search.yahoo.com/mrss/" /></entry><entry><title type="html">API Authentication Compared: API Keys, JWT, and OAuth 2.0</title><link href="http://localhost:4001/rest-api/authentication/security/2025/12/16/api-authentication-compared-api-keys-jwt-and-oauth-2-0.html" rel="alternate" type="text/html" title="API Authentication Compared: API Keys, JWT, and OAuth 2.0" /><published>2025-12-16T08:10:00-05:00</published><updated>2025-12-16T08:10:00-05:00</updated><id>http://localhost:4001/rest-api/authentication/security/2025/12/16/api-authentication-compared-api-keys-jwt-and-oauth-2-0</id><content type="html" xml:base="http://localhost:4001/rest-api/authentication/security/2025/12/16/api-authentication-compared-api-keys-jwt-and-oauth-2-0.html"><![CDATA[<p>API authentication answers a simple question: who is calling? The right answer depends on the kind of client, the sensitivity of the data, and how much delegation the system needs.</p>

<p>API keys, JWTs, and OAuth 2.0 are often discussed together, but they solve different problems.</p>

<h2 id="api-keys">API keys</h2>

<p>An API key is a secret string assigned to an application, account, or user.</p>

<p>Common request pattern:</p>

<div class="language-http highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="err">GET /v1/reports
Authorization: Bearer sk_live_123
</span></code></pre></div></div>

<p>Some APIs use a custom header:</p>

<div class="language-http highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="err">X-API-Key: sk_live_123
</span></code></pre></div></div>

<p>API keys are simple and work well for server-to-server integrations, internal tools, and low-friction developer onboarding.</p>

<h3 id="strengths">Strengths</h3>

<ul>
  <li>Easy to issue and use</li>
  <li>Good fit for scripts and backend services</li>
  <li>Easy to rotate if each client has its own key</li>
  <li>Simple to rate limit and audit by key</li>
</ul>

<h3 id="weaknesses">Weaknesses</h3>

<ul>
  <li>Usually long-lived</li>
  <li>Often copied into config files or CI secrets</li>
  <li>Do not represent delegated user consent by themselves</li>
  <li>Need careful storage and rotation</li>
</ul>

<p>Treat API keys like passwords. Store only hashed keys if possible, show the full key once, and support revocation.</p>

<h2 id="jwts">JWTs</h2>

<p>A JWT, or JSON Web Token, is a signed token that carries claims.</p>

<p>Example claims:</p>

<div class="language-json highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="p">{</span><span class="w">
  </span><span class="nl">"sub"</span><span class="p">:</span><span class="w"> </span><span class="s2">"usr_123"</span><span class="p">,</span><span class="w">
  </span><span class="nl">"iss"</span><span class="p">:</span><span class="w"> </span><span class="s2">"https://auth.example.com"</span><span class="p">,</span><span class="w">
  </span><span class="nl">"aud"</span><span class="p">:</span><span class="w"> </span><span class="s2">"api.example.com"</span><span class="p">,</span><span class="w">
  </span><span class="nl">"exp"</span><span class="p">:</span><span class="w"> </span><span class="mi">1765900000</span><span class="p">,</span><span class="w">
  </span><span class="nl">"scope"</span><span class="p">:</span><span class="w"> </span><span class="s2">"orders:read"</span><span class="w">
</span><span class="p">}</span><span class="w">
</span></code></pre></div></div>

<p>APIs commonly receive JWTs as bearer tokens:</p>

<div class="language-http highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="err">Authorization: Bearer eyJhbGciOi...
</span></code></pre></div></div>

<p>The API verifies the token signature, checks expiration, validates issuer and audience, then uses the claims for authorization decisions.</p>

<h3 id="strengths-1">Strengths</h3>

<ul>
  <li>Self-contained claims can reduce auth server lookups</li>
  <li>Works well across services</li>
  <li>Supports short-lived access tokens</li>
  <li>Common in modern identity systems</li>
</ul>

<h3 id="weaknesses-1">Weaknesses</h3>

<ul>
  <li>Harder to revoke before expiration</li>
  <li>Easy to validate incorrectly</li>
  <li>Token size can grow quickly</li>
  <li>Claims can become stale if user permissions change</li>
</ul>

<p>JWTs are not automatically secure. The value comes from correct validation, short lifetimes, and careful claim design.</p>

<h2 id="oauth-20">OAuth 2.0</h2>

<p>OAuth 2.0 is an authorization framework. It lets a user grant one application access to resources without sharing their password.</p>

<p>A typical web app flow looks like:</p>

<ol>
  <li>The client redirects the user to the authorization server.</li>
  <li>The user signs in and grants consent.</li>
  <li>The client receives an authorization code.</li>
  <li>The client exchanges the code for tokens.</li>
  <li>The API receives an access token on each request.</li>
</ol>

<p>OAuth 2.0 access tokens are often JWTs, but they do not have to be. OAuth describes the flow and responsibilities, not just the token format.</p>

<h3 id="strengths-2">Strengths</h3>

<ul>
  <li>Best fit for third-party delegated access</li>
  <li>Supports scopes and consent</li>
  <li>Separates authorization server from resource API</li>
  <li>Mature patterns for web, mobile, and device clients</li>
</ul>

<h3 id="weaknesses-2">Weaknesses</h3>

<ul>
  <li>More moving parts than API keys</li>
  <li>Requires careful redirect URI and client configuration</li>
  <li>Easy to choose the wrong grant type</li>
  <li>Overkill for simple internal APIs</li>
</ul>

<p>For browser and mobile apps, use Authorization Code with PKCE. Avoid old implicit-flow patterns for new applications.</p>

<h2 id="which-one-should-you-choose">Which one should you choose?</h2>

<p>For server-to-server integrations, API keys are often enough if they are scoped, rate limited, and easy to rotate.</p>

<p>For first-party apps with a central identity provider, short-lived JWT access tokens are common and practical.</p>

<p>For third-party apps accessing user data, OAuth 2.0 is the right model because it handles consent, scopes, and delegation.</p>

<h2 id="authorization-still-matters">Authorization still matters</h2>

<p>Authentication identifies the caller. Authorization decides what the caller can do.</p>

<p>After validating a credential, still check:</p>

<ul>
  <li>Does this caller have access to this account or resource?</li>
  <li>Does the token or key include the required scope?</li>
  <li>Is the action allowed by role or policy?</li>
  <li>Has the credential been revoked or disabled?</li>
</ul>

<p>A valid token should not mean unlimited access.</p>

<h2 id="operational-basics">Operational basics</h2>

<p>Whatever scheme you choose, support:</p>

<ul>
  <li>TLS only</li>
  <li>Secret rotation</li>
  <li>Credential revocation</li>
  <li>Audit logs</li>
  <li>Rate limits</li>
  <li>Least-privilege scopes</li>
  <li>Clear <code class="language-plaintext highlighter-rouge">401</code> and <code class="language-plaintext highlighter-rouge">403</code> errors</li>
</ul>

<p>Most authentication incidents come from operational gaps, not from the choice of token format alone.</p>

<h2 id="closing-thoughts">Closing thoughts</h2>

<p>API keys are simple, JWTs are portable, and OAuth 2.0 handles delegated access. They can also work together: an OAuth access token may be a JWT, and an internal service may still use an API key for automation.</p>

<p>Choose the smallest model that fits the trust boundary. Then invest in validation, rotation, scopes, and monitoring. That is where authentication becomes reliable in production.</p>]]></content><author><name>Sean</name></author><category term="rest-api" /><category term="authentication" /><category term="security" /><summary type="html"><![CDATA[API authentication answers a simple question: who is calling? The right answer depends on the kind of client, the sensitivity of the data, and how much delegation the system needs.]]></summary><media:thumbnail xmlns:media="http://search.yahoo.com/mrss/" url="http://localhost:4001/assets/images/api-authentication-compared-api-keys-jwt-and-oauth-2-0.png" /><media:content medium="image" url="http://localhost:4001/assets/images/api-authentication-compared-api-keys-jwt-and-oauth-2-0.png" xmlns:media="http://search.yahoo.com/mrss/" /></entry><entry><title type="html">CORS Explained for API Developers</title><link href="http://localhost:4001/rest-api/http/cors/2025/11/22/cors-explained-for-api-developers.html" rel="alternate" type="text/html" title="CORS Explained for API Developers" /><published>2025-11-22T11:05:00-05:00</published><updated>2025-11-22T11:05:00-05:00</updated><id>http://localhost:4001/rest-api/http/cors/2025/11/22/cors-explained-for-api-developers</id><content type="html" xml:base="http://localhost:4001/rest-api/http/cors/2025/11/22/cors-explained-for-api-developers.html"><![CDATA[<p>CORS errors are frustrating because the API can work perfectly in curl while failing in the browser. That is the clue: CORS is a browser security feature, not a general HTTP feature.</p>

<p>If you build APIs that browsers call directly, you need to understand what the browser is asking for and which headers your server must return.</p>

<h2 id="what-cors-protects">What CORS protects</h2>

<p>Browsers enforce the same-origin policy. A script loaded from one origin cannot freely read responses from another origin.</p>

<p>An origin is the combination of:</p>

<ul>
  <li>Scheme: <code class="language-plaintext highlighter-rouge">https</code></li>
  <li>Host: <code class="language-plaintext highlighter-rouge">app.example.com</code></li>
  <li>Port: <code class="language-plaintext highlighter-rouge">443</code></li>
</ul>

<p>These are different origins:</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>https://app.example.com
https://api.example.com
http://app.example.com
https://app.example.com:8443
</code></pre></div></div>

<p>CORS, short for Cross-Origin Resource Sharing, is the mechanism that lets a server opt in to being called from another origin.</p>

<h2 id="simple-requests">Simple requests</h2>

<p>Some browser requests are considered simple. For example:</p>

<div class="language-http highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="err">GET /users/me
Origin: https://app.example.com
</span></code></pre></div></div>

<p>If the API allows that origin, it responds with:</p>

<div class="language-http highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="err">Access-Control-Allow-Origin: https://app.example.com
</span></code></pre></div></div>

<p>The browser checks that header before making the response available to JavaScript. If the header is missing or does not match, the browser blocks the response even if the server returned <code class="language-plaintext highlighter-rouge">200 OK</code>.</p>

<h2 id="preflight-requests">Preflight requests</h2>

<p>Many API calls trigger a preflight request. The browser sends an <code class="language-plaintext highlighter-rouge">OPTIONS</code> request first to ask whether the real request is allowed.</p>

<p>Example:</p>

<div class="language-http highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="err">OPTIONS /users/me
Origin: https://app.example.com
Access-Control-Request-Method: PATCH
Access-Control-Request-Headers: authorization, content-type
</span></code></pre></div></div>

<p>The API should respond:</p>

<div class="language-http highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">HTTP</span><span class="o">/</span><span class="m">1.1</span> <span class="m">204</span> <span class="ne">No Content</span>
<span class="na">Access-Control-Allow-Origin</span><span class="p">:</span> <span class="s">https://app.example.com</span>
<span class="na">Access-Control-Allow-Methods</span><span class="p">:</span> <span class="s">GET, POST, PATCH, DELETE</span>
<span class="na">Access-Control-Allow-Headers</span><span class="p">:</span> <span class="s">Authorization, Content-Type</span>
<span class="na">Access-Control-Max-Age</span><span class="p">:</span> <span class="s">600</span>
</code></pre></div></div>

<p>If the preflight succeeds, the browser sends the real <code class="language-plaintext highlighter-rouge">PATCH</code> request.</p>

<h2 id="common-cors-headers">Common CORS headers</h2>

<p>The headers you will use most often are:</p>

<table>
  <thead>
    <tr>
      <th>Header</th>
      <th>Purpose</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">Access-Control-Allow-Origin</code></td>
      <td>Which origin may read the response</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">Access-Control-Allow-Methods</code></td>
      <td>Which methods are allowed for preflighted requests</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">Access-Control-Allow-Headers</code></td>
      <td>Which request headers are allowed</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">Access-Control-Allow-Credentials</code></td>
      <td>Whether cookies or HTTP auth may be included</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">Access-Control-Expose-Headers</code></td>
      <td>Which response headers JavaScript may read</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">Access-Control-Max-Age</code></td>
      <td>How long the browser may cache preflight results</td>
    </tr>
  </tbody>
</table>

<p>Most CORS bugs come from a missing allowed header, an origin mismatch, or credentials being configured incorrectly.</p>

<h2 id="be-careful-with-credentials">Be careful with credentials</h2>

<p>Credentialed requests include cookies, client certificates, or HTTP authentication. In <code class="language-plaintext highlighter-rouge">fetch</code>, that usually means:</p>

<div class="language-javascript highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nf">fetch</span><span class="p">(</span><span class="dl">"</span><span class="s2">https://api.example.com/users/me</span><span class="dl">"</span><span class="p">,</span> <span class="p">{</span>
  <span class="na">credentials</span><span class="p">:</span> <span class="dl">"</span><span class="s2">include</span><span class="dl">"</span>
<span class="p">});</span>
</code></pre></div></div>

<p>For this to work, the server must return:</p>

<div class="language-http highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="err">Access-Control-Allow-Origin: https://app.example.com
Access-Control-Allow-Credentials: true
</span></code></pre></div></div>

<p>You cannot combine credentialed requests with:</p>

<div class="language-http highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="err">Access-Control-Allow-Origin: *
</span></code></pre></div></div>

<p>The server must echo or select a specific allowed origin.</p>

<h2 id="do-not-use-cors-as-authentication">Do not use CORS as authentication</h2>

<p>CORS limits which browser origins can read responses. It does not prove who the user is, and it does not stop non-browser clients from calling your API.</p>

<p>Your API still needs real authentication and authorization:</p>

<ul>
  <li>Session cookies</li>
  <li>Bearer tokens</li>
  <li>API keys</li>
  <li>OAuth 2.0 access tokens</li>
</ul>

<p>CORS is a browser access control layer. It is not a security model for your entire API.</p>

<h2 id="configure-allowed-origins-explicitly">Configure allowed origins explicitly</h2>

<p>For production, prefer an allowlist:</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>https://app.example.com
https://admin.example.com
</code></pre></div></div>

<p>Avoid reflecting any incoming <code class="language-plaintext highlighter-rouge">Origin</code> header without checking it. That turns CORS into “allow everyone” while making the configuration look more controlled than it is.</p>

<p>For local development, add explicit local origins:</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>http://localhost:3000
http://localhost:5173
</code></pre></div></div>

<p>Keep development origins out of production if they are not needed there.</p>

<h2 id="debugging-checklist">Debugging checklist</h2>

<p>When a browser reports a CORS error, check:</p>

<ul>
  <li>Is the request coming from the origin you expect?</li>
  <li>Does the response include <code class="language-plaintext highlighter-rouge">Access-Control-Allow-Origin</code>?</li>
  <li>For preflight, does the <code class="language-plaintext highlighter-rouge">OPTIONS</code> route return success?</li>
  <li>Are the requested method and headers allowed?</li>
  <li>Are credentials enabled on both client and server if cookies are used?</li>
  <li>Is the API redirecting the preflight request?</li>
  <li>Is a proxy or CDN stripping CORS headers?</li>
</ul>

<p>Use the browser network panel. The console message is helpful, but the request and response headers tell the real story.</p>

<h2 id="closing-thoughts">Closing thoughts</h2>

<p>CORS is easier once you separate the browser rule from the API behavior. The server decides which origins, methods, and headers are allowed. The browser enforces that decision before JavaScript can read the response.</p>

<p>Start with a small allowlist, handle <code class="language-plaintext highlighter-rouge">OPTIONS</code> correctly, and test both simple and credentialed requests. That covers most real-world CORS issues without weakening the API.</p>]]></content><author><name>Sean</name></author><category term="rest-api" /><category term="http" /><category term="cors" /><summary type="html"><![CDATA[CORS errors are frustrating because the API can work perfectly in curl while failing in the browser. That is the clue: CORS is a browser security feature, not a general HTTP feature.]]></summary><media:thumbnail xmlns:media="http://search.yahoo.com/mrss/" url="http://localhost:4001/assets/images/cors-explained-for-api-developers.png" /><media:content medium="image" url="http://localhost:4001/assets/images/cors-explained-for-api-developers.png" xmlns:media="http://search.yahoo.com/mrss/" /></entry></feed>